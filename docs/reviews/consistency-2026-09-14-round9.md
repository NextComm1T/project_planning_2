# 정합성 재감사 — 9차 (이슈 #25)

읽기 테스트 코치 질문 31개 반영(9차) 뒤 돌린 재감사 기록이다. 결정 자체가 아니라 **문서 사이가 어긋난 곳**만 본다 — 결정 내용과 반영 위치는 00-backlog.md 의 「(2026-09-11 9차)」 참고 이력 7줄에 있다.

- **감사 범위**: 01 · 03 · 04 · 05 · 06 · 07 · PRD (00-backlog.md 는 참고 이력 형식만)
- **방법**: `doc-consistency-auditor` 에이전트에 7개 항목을 지정해 좁게 돌리고, 지적마다 원문을 직접 대조해 확인했다. 전수 감사를 돌린 1차 시도는 시간 초과로 중단됐고, 그 자리를 아래 기계 검증 5종이 메운다.
- **집계**: BLOCKER 1 · MAJOR 1 · MINOR 2 — **모두 이번 9차 편집이 만든 것이며, 모두 고쳤다. 남은 BLOCKER 0.**
- **날짜 주의**: 9차 판정과 문서 본문의 날짜 표기는 결정이 내려진 **2026-09-11** 이고, 이 재감사와 후속 수정은 **2026-09-14** 다. 00-backlog 후속 이력 줄에 두 날짜를 함께 적었다.

---

## 고친 것 (4건 — 전부 9차 편집이 만든 것)

| 등급 | 무엇이 어긋났나 | 어떻게 고쳤나 |
| ---- | -------------- | ------------- |
| BLOCKER | **페이스 단위** — 9차가 "(속력) → 분/km" 를 03 · 06 · 07 에는 반영했는데 04 두 칸이 옛 표기로 남았다. `04 F2 결과` 「현재 페이스(속력 — 최근 15초 기준)」 는 03 R17(분/km)과, `04 F9 입력` 「세션 총 거리·시간으로 계산한 속력값」 은 같은 행의 결과 ⑤(분/km)와 어긋났다. 속력(km/h)과 페이스(분/km)는 역수라 그대로 구현하면 다른 숫자가 나온다 | 두 칸 모두 **분/km** 로 통일 |
| MAJOR | **종료 처리 실패 시 세션 상태가 어느 문서에도 없었다.** P14 로 저장 실패를 MVP 에 넣었지만 실패한 세션이 "진행 중"으로 남는지 "종료"가 되는지가 없어 P4(종료 전 결과 화면 접근 불가)를 판정할 수 없었다. "진행 중"으로 남는다고 읽으면 P7(진행 중 1개)·P12(진행 중이면 로그아웃·탈퇴 불가)에 걸려 사용자가 다음 러닝도 로그아웃도 못 한다 | 05 상태값 「러닝 세션」에 **"진행 중→종료 전이는 종료 버튼을 누른 시점에 일어나며 저장·순위 계산의 성공 여부와 무관하다"** 를 명시. 07 흐름표의 실패 행에도 「진행 중 → 종료」를 넣어 앞의 두 종료 행과 표기를 맞췄다 |
| MINOR | 07 결과 화면 「보이는 것」이 다른 예외 문구("랭킹 미반영" 등)는 다 열거하면서 04 F9 ⑤ · 06 의 **"총 러닝 거리 0이면 페이스 —"** 만 빠졌다 | 07 결과 화면 평균 페이스에 예외를 적었다. 기록 탭 · 기록 상세는 같은 예외를 다시 적지 않고 06 「러닝 세션 · 평균 페이스」 칸 정의를 따른다(07 머리말의 표기 규칙대로) |
| MINOR | 04 F5 의 「본인 줄 강조」에 `· P5` 가 붙어 있는데 05 P5 에는 강조 규칙이 없다 — 없는 근거를 가리켰다 | `· P5` 를 앞 문장(인정 거리 0 초과 · 전체 표시 — 실제로 P5 에 있는 내용)으로 옮기고, 강조는 **05 에 없는 화면 규칙이라 04 와 07 이 단일 기준**임을 문장 안에 밝혔다 |

## 이상 없음으로 확인된 것

- **카운트다운 종료 시 전이** — 03 R16 · 04 F1 · 05 상태값 · 06(세션 상태 · 러닝 시간 기산점) · 07 흐름표 5곳이 같은 문장이다. 읽기 테스트가 짚었던 05 ↔ 03 R16 충돌은 닫혔다.
- **닉네임 거절 문구 3종** — 04 F10 · 05 P10 · 07 닉네임 설정 화면 세 곳의 문구가 글자까지 같다. 06 닉네임 칸과 07 설정 화면은 문구를 다시 적지 않고 P10 에 위임한다.
- **P14 전파** — 04(F2·F6·F9) · 06 비기능 절 · 07(결과 화면 · 흐름표) · PRD MVP 표가 05 P14 정의와 문구·대상 F 모두 일치.
- **00-backlog 9차 이력 7줄** — 01 · 03 · 04 · 05 · 06 · 07 · PRD 섹션에 하나씩, 7줄 모두 「반영 대상: … (이슈 #25).」 로 끝난다.
- **07 흐름표 중복 행** — 38행의 출발·도착 쌍을 전수 대조. 「랭킹 화면 → 기록 탭」은 공통 탭 이동 1행으로 합쳐졌고 남은 중복 없음. 출발·도착이 겹치는 나머지 쌍은 트리거가 다른 별개 경로다.

## 기계 검증 5종 (scratchpad 스크립트 · 저장소에 커밋하지 않음)

전부 통과.

1. ID 참조가 현행 범위(S0~S8 · R1~R26 · F1~F13 · **P1~P14** · SP1~SP3 · A1) 밖을 가리키지 않는다 — `구 ` · `1차 ` · `원 ` 세대 표기는 제외
2. 03~07 · PRD 본문 `[제안]` 태그 0건 (머리말 · 표 헤더 · PRD 확정 기록 라벨 제외)
3. 07 화면 목록 F 합집합 = F1~F13 · PRD MVP 13행 = F1~F13 · PRD 기능 이름 = 04 트리거 문자열
4. 표 열 수 — 02 5·6 · 03 6 · 04 10·5 · 05 6·2 · 06 4·3 · 07 5·4·3 · PRD 6·3·3·3 (모든 행이 직사각형)
5. 머리말 `[?]` 개수 = 본문 실제 개수 (06 3 · 07 5 · PRD 9 · 나머지 0)

추가로 **04 「정책(P)」 열 13행 == PRD MVP 표 P 열 13행** 을 따로 대조해 전부 일치를 확인했다.

## 남겨 둔 것 (이번 범위 밖)

- **04 「인수 조건」 13칸 · 「우선순위 2축」 표** — 코치 질문 28 · 29. 별도 작업.
- **PRD 열린 질문 9개** — GPS 저장 주기 · Zone 구역 범위(9차 신규 · 팀이 지도에서 정한다) · Zone 좌표 형태·정밀도 · 07 디자인 절 5칸 · 확정 기록 이름.
- **P14 의 효과가 걸리는 화면** — "저장 성공 전까지 기록 탭·랭킹에 나타나지 않는다"가 실제로 영향을 주는 화면은 F5 · F7 인데, P14 의 대상 F 는 F2 · F6 · F9 이라 04 F5·F7 의 정책 열에는 P14 가 없다. 04 정책 열은 05 「대상 F」 의 역산이라는 규칙과는 정합하므로 불일치로 세지 않았다. 05 P14 의 대상 F 를 넓힐지는 팀 판단.
- **"다시 시도"의 재시도 동작·횟수** — 어느 문서에도 없다. 기획 코치 영역.
- **이전 라운드 미종결 MINOR** — 「탄천 인정 누적 거리」(세션)와 「탄천 Ranking Zone 인정 누적 거리」(전체)의 이름 혼동 등은 `consistency-2026-09-11-round6.md` 참고. 이번에 건드리지 않았다.
