# 문서 정합성 감사 — 2026-09-11

**판정**: FAIL (BLOCKER 2건)
**범위**: `docs/00-backlog` + `docs/01`~`06-data` **7개** (`07-screens.md`·`PRD.md`·`docs/drafts/` 없음 확인) · A 1 · S 9(S0~S8) · R 23 · F 11 · P 10 · SP 2 · ID 토큰 출현 1,038건(01~06 본문 478 / 00-backlog 560)
**기준**: `8469e1b docs: GPS 이동 경로 영구 저장 결정을 01~06에 일괄 반영` (+ 미커밋 변경: 00·04·05·06)
**결과**: BLOCKER 2 · MAJOR 11 · MINOR 12 · 확인 필요 5

## 검사 커버리지

| 검사 | 모집단 | 검사 완료 | 발견 |
|---|---|---|---|
| C1 참조 무결성 | 485 (인라인 478 + 상대 링크 7) | 485 | 1건 |
| C2 재정렬 잔존 참조 | 1,038 (01~06 478 + 00 560) | 1,038 | 1건 |
| C3 양방향 커버리지 | 53 (R23·F11·P10·SP2·S9) | 53 | 3건 |
| C4 후순위/MVP 상태 | 44 (백로그 행) + 01 MVP 서술 8 | 52 | 5건 |
| C5 헤더 메타 | 7 (문서) | 7 | 3건 |
| C6 값·용어 | 15 (용어 5 · 수치 6 · 화면 항목 4) | 15 | 4건 |
| C7 흐름·상태값 | 29 (mermaid 노드 17 · S 9 · 상태값군 3) | 29 | 3건 |
| C8 변경 전파 | 33 (00 참고 이력 28 · 최근 커밋 5) | 33 | 4건 |

- 존재하지 않는 ID 참조 **0건**, 번호 구멍 **0건**(R1~R23·F1~F11·P1~P10·S0~S8 연속), **항목 유실 0건**(03 생존 16+R17~R23 / 04 F1~F10+F11 / 05 P1~P9+P10 모두 00-backlog에 등재됨).
- 04의 「정책(P)」·「우선순위 2축」·「인수 조건」 공란은 지시대로 보고에서 제외했다. 이 때문에 C3의 **역방향 교차(05→04 반영 확인)** 는 판정 불가로 남겼다.
- 06의 판정 대기 `[제안]` 5건(06:17·27·28·39·52)과 `[?]` 2건(06:32·46)은 불일치로 세지 않았다.

---

## 발견 사항

### F-01 · BLOCKER · C6 값 상충 / C8 전파 누락

**무엇**: 3차 P9 확대(Zone 안팎 무관·총 러닝 거리에서도 제외)가 03에 전혀 전파되지 않아, 제약 요구사항 R12와 정면으로 충돌한다.

**증거**:
- `05-policy.md:21` — P9 "**Ranking Zone 안팎을 가리지 않는다** … 총 러닝 거리와 탄천 인정 거리 두 값 모두의 계산에서 제외"
- `03-requirements.md:24` — R12(제약·중요도 상) "탄천 Ranking Zone **외부에서 측정된 거리도** 개인 러닝 기록에는 **포함되어야 한다**" — 예외 문구 없음
- `03-requirements.md:18` — R6(제약·상)은 GPS 끊김 구간 제외만 규정, 속도 초과 제외 없음
- `03-requirements.md:20` — R8 저장 대상에 "속도 초과 지점은 제외 사유와 함께 저장" 규칙 없음. 반면 `04-features.md:20`·`06-data.md:34`는 저장한다고 명시
- `00-backlog.md:258` — 3차 전파 목록에 05·04·06만 있고 03이 없다

**왜 문제인가**: Zone 밖 25km/h 초과 구간을 총 거리에 넣을지(R12) 뺄지(P9) 구현자가 결정하게 되고, 개인 기록 총 거리 값이 문서마다 달라진다.

**제안**: `03-requirements.md:24` R12 끝에 "단 비정상 이동 속도 구간은 제외한다 · P9"를, `03:18` R6와 `03:20` R8에 각각 속도 초과 구간의 거리 제외·지점 저장 규칙을 덧붙인다. (R11 `03:23`은 "내부 거리만 반영"이라 P9와 충돌하지 않는다 — 수정 불필요.)

### F-02 · BLOCKER · C2 재정렬 잔존 참조

**무엇**: 00-backlog의 제거 목록이 접두어 없는 옛 번호 `R20`·`R22`·`R23`을 쓰는데, 2차에서 같은 번호가 **현행 MVP 요구사항으로 신설**되어 "후순위"로 잘못 읽힌다.

**증거**:
- `00-backlog.md:113` — `R20` = TOP100 캡(후순위) / `03-requirements.md:32` — 현재 R20 = **시작 전 지도**(MVP)
- `00-backlog.md:114` — `R22` = 지난주 기록 비교(후순위) / `03-requirements.md:34` — 현재 R22 = **닉네임 확인·수정**(MVP)
- `00-backlog.md:115` — `R23` = 지난달 기록 비교(후순위) / `03-requirements.md:35` — 현재 R23 = **누적 거리 2종**(MVP)
- `00-backlog.md:43` — 02 섹션도 "S6 · R22·R23·R24(제거)"로 같은 충돌
- `00-backlog.md:74` — 문서 자체 규약은 "구R_ 표기는 R1~R4·R10·R12~R16"만 선언 → R17 이후 신설분을 덮지 못함

**왜 문제인가**: 백로그를 후순위 판정 기준으로 읽으면 R20·R22·R23(시작 전 지도·닉네임 수정·누적 거리 2종)이 MVP에서 통째로 빠진다.

**제안**: `00-backlog.md:113·114·115·43`의 `R20`·`R22`·`R23`·`R24`를 `구R20`·`구R22`·`구R23`·`구R24`로 바꾸고, `00-backlog.md:74`의 규약 문장에 "R17 이후 신설분과 겹치는 옛 번호도 구R_로 표기한다"를 추가한다.

### F-03 · MAJOR · C3 R→F 커버리지

**무엇**: R11·R12가 04의 **R 열**에 한 번도 없고 산문 안에서만 언급된다.

**증거**: `03-requirements.md:23`(R11) · `:24`(R12) — 둘 다 유형 제약·중요도 상 / `04-features.md:23` F9 결과 칸 "R11 반영 규칙에 따름" · `04-features.md:20` F6 결과 칸 "Zone 외부 거리도 포함하고(R12)" — R 열은 각각 `R14`, `R8`뿐

**왜 문제인가**: F 단위로 개발 티켓을 끊으면 두 제약이 어느 기능의 인수 조건인지 추적되지 않는다.

**제안**: `04-features.md:20` F6의 R 열을 `R8, R12`로, `:23` F9의 R 열을 `R14, R11`로 보강한다.

### F-04 · MAJOR · C3 F→P 커버리지

**무엇**: F8(소셜 로그인)이 05의 어떤 P의 「대상 F」에도 없다. MVP 11개 F 중 유일하다.

**증거**: `04-features.md:22` F8 / `05-policy.md:13~22` 대상 F 전수 = F1,F2,F3,F4,F5,F6,F7,F9,F10,F11 — F8 없음 / `06-data.md:14·15`는 F8에 대해 "인증 성공 시에만 저장", "재로그인 때 UID를 찾아 잇는다"는 규칙을 이미 규정

**왜 문제인가**: 06이 정책 근거 없이 인증 저장·계정 연결 규칙을 단독 규정하게 되어, 인증 실패·중복 계정 처리 기준이 05에 없다.

**제안**: `05-policy.md`에 대상 F = F8인 정책 1행(인증 성공 전에는 사용자 생성 불가 · 동일 소셜 계정 식별자는 하나의 UID에만 연결)을 추가한다.

### F-05 · MAJOR · C8 전파 누락 (3차 UID)

**무엇**: 3차 UID 결정이 06에만 존재하고, UID **발급 시점**과 **재로그인 흐름**이 02·03·04 어디에도 없다.

**증거**:
- `06-data.md:13` — "가입 시 한 번 발급되고 이후 절대 바뀌지 않는 고유 값" / `06-data.md:15` — "재로그인 때 이 값으로 사용자 UID를 찾아 같은 계정으로 잇는다"
- `04-features.md:22` F8 결과 칸 — "인증 성공 시 회원가입이 완료되고 닉네임 설정 단계(F10)로 이동"뿐. 트리거도 "앱 **최초 실행 후** 회원가입 화면 진입"만이라 재로그인 경로가 없다
- `02-workflow.md:23` S0 — 가입만 서술, 재로그인 없음
- `00-backlog.md:132` — "03에는 새 R을 만들지 않았다"

**왜 문제인가**: "다시 로그인하면 내 기록이 이어진다"는 사용자가 체감하는 동작인데 상위 문서에 흐름·요구·기능이 전혀 없어, F8 구현 범위가 최초 가입만으로 좁혀진다.

**제안**: `04-features.md:22` F8의 트리거에 "② 재로그인", 결과에 "최초 가입 시 UID 발급 / 재로그인 시 소셜 계정 식별자로 기존 UID 연결"을 추가한다. UID 자체를 R로 만들지 않는 판단은 「확인 필요」로 넘긴다.

**참고**: 닉네임으로 사용자를 가리키는 잔여 칸은 **없다** — `06:13·16·18·35·40` 모두 UID로 통일되어 있고 닉네임 칸(`06:16`)은 식별자 금지를 명시한다.

### F-06 · MAJOR · C8 전파 누락 (3차 P9)

**무엇**: P9가 Zone 판정과 무관해졌는데 06의 Ranking Zone 좌표 칸이 여전히 P9를 Zone 내부 판정 사용처로 적는다.

**증거**: `06-data.md:46` — "R11·P5·**P9**의 Zone 내부 판정 … 이 값을 쓴다" / `05-policy.md:21` — P9 "Zone 안팎을 가리지 않는다" / `00-backlog.md:258` 전파 대상 목록에 06:46이 빠져 있음

**제안**: `06-data.md:46`에서 `P9`를 지운다(속도 판정은 `06:32` 기록 시각 기준이지 좌표 기준이 아니다).

### F-07 · MAJOR · C8 전파 누락 (닉네임 제한)

**무엇**: 닉네임 9자·문자 종류 제한이 02 S0에 반영되지 않았는데, S8이 "가입 때와 같은 정책"을 참조한다.

**증거**: `02-workflow.md:23` S0 To-Be — "닉네임을 직접 설정한다(필수). 닉네임은 다른 사용자와 중복될 수 없다" (9자·문자 종류 없음) / `02-workflow.md:31` S8 — "**가입 때와 같은 정책**(필수 · 9자 이하 · 한글·영문·숫자만 · 공백·특수문자 불가 · 중복 불가)" / `00-backlog.md:134` 전파 대상에 02가 없음

**왜 문제인가**: S8이 가리키는 "가입 때 정책"이 S0에 존재하지 않아 참조가 공중에 뜬다.

**제안**: `02-workflow.md:23` S0 To-Be의 중복 불가 문구 뒤에 "9자 이하 · 한글·영문·숫자만"을 추가한다.

### F-08 · MAJOR · C4 낡은 MVP 서술 / C8

**무엇**: 01의 「MVP 핵심 워크플로우」가 2차 결정(경로 영구 저장·기록 상세)과 S0·S6·S7·S8을 담지 않은 3단계 그대로다.

**증거**: `01-problem.md:42~58` — ①러닝 기록 ②GPS 구역 판정 ③결과 분리 및 랭킹 반영 (가입·닉네임, 기록 탭, 기록 상세, 설정 없음) / `02-workflow.md:23~31` S0~S8 9단계 / 커밋 `8469e1b` 제목은 "01~06에 일괄 반영"이나 실제 변경 파일에 `docs/01-problem.md` **없음**

**왜 문제인가**: 01이 상위 범위 기준 문서인데 MVP를 3단계로 좁혀 서술해, 범위 판정 시 S7 기록 상세·S0 가입이 빠질 수 있다.

**제안**: `01-problem.md:42` 절에 "④ 기록 저장 및 다시 보기(경로 포함)"를 최소 한 줄로 추가하고, 가입·설정은 보조 흐름으로 한 줄 명시한다.

### F-09 · MAJOR · C4/C6 판정 시점 상충

**무엇**: 01이 Zone 판정을 "러닝 종료 시"로 서술하는데, 02~04는 러닝 중 실시간 계산이다.

**증거**: `01-problem.md:50` — "**러닝 종료 시** 시스템이 유저의 GPS 이동 경로를 분석하여 … 필터링한다" / `03-requirements.md:16` R4 "러닝 중 현재 탄천 인정 누적 거리를 확인" / `04-features.md:16` F2 "탄천 인정 누적 거리 … 실시간으로 갱신" / `02-workflow.md:25` S2

**제안**: `01-problem.md:50`을 "러닝 중 실시간으로 판정하고 종료 시 확정한다"로 고친다.

### F-10 · MAJOR · C4 낡은 MVP 서술

**무엇**: 01이 결과 화면에 "순위 **상승** 결과"를 보여준다고 하나, 현행 결과 화면에 상승폭 개념이 없다.

**증거**: `01-problem.md:58` — "이후 유저에게 자신의 **순위 상승 결과**를 화면에 보여준다" / `03-requirements.md:26` R14·`04-features.md:23` F9 — ③ 개인 순위(누적 거리 기준) 현재값만 / `00-backlog.md:194·195` 주간·월간 순위 **변화** 표시는 후순위

**제안**: `01-problem.md:58`을 "자신의 현재 순위를 화면에 보여준다"로 고친다.

### F-11 · MAJOR · C8 규칙 상충 (번호 규칙)

**무엇**: 03·04·05 머리말이 아직 "빈 번호 없이 재정렬한다"라고 선언한다. 2차부터 팀 규칙은 재정렬 금지다.

**증거**: `03-requirements.md:5` · `04-features.md:5` · `05-policy.md:5` — "남은 항목은 빈 번호 없이 재정렬한다" / `00-backlog.md:133` "기존 R1~R19는 번호를 그대로 두고 재정렬하지 않았다" · `:217` 동일(F) · `:259` "번호는 새로 매기지 않았다" · `:210` "「번호는 절대 재정렬하지 않는다」 규칙"

**왜 문제인가**: 다음 편집자가 머리말대로 재정렬하면 F-02 유형의 조용한 오참조가 전 문서에 한꺼번에 생긴다.

**제안**: 세 머리말 문장을 "제거·후순위 항목은 00-backlog.md로 옮기되 **남은 항목의 번호는 재정렬하지 않고 신규는 뒤에 붙인다**(2026-09-11 2차 변경)"로 교체한다.

### F-12 · MAJOR · C7 미정의 상태값

**무엇**: 05 「언제(상태값)」 열에 05 「상태값」 절에 정의되지 않은 값이 쓰인다.

**증거**: `05-policy.md:18` P6 "상시" · `:20` P8 "두 사용자의 러닝 종료 처리가 동시에 들어올 때" · `:22` P10 "① 회원가입 중 최초 닉네임 설정 ② 가입 후 설정 화면에서 닉네임 수정" / 정의된 값은 `05-policy.md:26~28`의 러닝 세션 3종·GPS 신호 3종·랭킹 집계 1종뿐 — 가입·프로필(S0·S8, F8·F10) 대응 상태값이 없다

**제안**: `05-policy.md:26~28`에 "계정: 미가입 → 가입 완료(닉네임 설정 완료)" 상태를 추가하거나, P6·P8·P10의 열 값을 정의된 상태값으로 바꾼다.

### F-13 · MAJOR · C4/C6 백로그의 현재형 상충 (페이스)

**무엇**: 00-backlog의 페이스 관련 3개 기록이 "후순위"라고 현재형으로 단정하는데, 페이스는 현행 MVP에 전면 포함돼 있다.

**증거**:
- `00-backlog.md:41` — "페이스 기록 및 관련 기능 전반(**실시간 표시**·음성 안내 포함) … 후순위" / `03-requirements.md:29` R17·`04-features.md:16` F2는 러닝 중 페이스 실시간 표시가 MVP
- `00-backlog.md:44` — "러닝 완료 후 평균 페이스 표시 … 후순위" / `03-requirements.md:26` R14 ·`04-features.md:23` F9 ⑤ 페이스
- `00-backlog.md:54` — "S4의 '평균 페이스' … 후순위 전환되어 To-Be 문구와 mermaid에서 제거했다" / `02-workflow.md:27` S4 To-Be·`:62` mermaid S4 노드 둘 다 페이스 포함
- 같은 문서 `00-backlog.md:128`(R17 신설)·`:125`(R34 페이스는 R14로 통합)이 위 3행과 모순

**제안**: `00-backlog.md:41`의 후보명을 "음성 페이스 안내"로 좁히고, `:44`·`:54`에 "(2026-09-10 이후 R14·R17로 MVP 복귀)"를 덧붙인다.

### F-14 · MINOR · C5 헤더 메타 부재

**증거**: `01-problem.md:1~2` — 다른 6개 문서와 달리 `> 문서: … · 상태: … · 마지막 갱신: …` 줄이 없다(`02:3`·`03:3`·`04:3`·`05:3`·`06:3`·`00:3` 대비).

**제안**: `01-problem.md:2`에 동일 형식 헤더를 넣는다(마지막 갱신 2026-09-11).

### F-15 · MINOR · C5 헤더 [제안] 개수 검증 불가

**증거**: `03-requirements.md:3` "채택한 [제안] 3개(R9·R10·R13)" / `04-features.md:3` "3개(F5·F7·F8)" / `05-policy.md:3` "3개(P7·P8·P9)" — 세 문서 본문에 `[제안]` 표기가 **0건**(열 제목 `출처(S · 인터뷰 · [제안] 근거)` 제외)이라 열거된 번호를 본문에서 대조할 수 없다. 06(`06:3`)은 본문과 일치한다.

**제안**: 03 R9·R10·R13, 04 F5·F7·F8, 05 P7·P8·P9의 출처 칸에 `[제안·채택]` 표기를 남긴다.

### F-16 · MINOR · C5/C1 백로그 범위 표기

**증거**: `00-backlog.md:3` "목적: **01~05 문서**에서 제거되었거나 후순위로 전환된 항목" / `06-data.md:60`은 이 문서를 가리키고, `00-backlog.md`에는 `## 06-data.md` 섹션이 없다. 3차 UID 기록도 `00-backlog.md:132`로 03 섹션 안에 들어가 있다.

**제안**: `00-backlog.md:3`을 "01~06 문서"로 고치고 `## 06-data.md` 섹션을 만든 뒤 `:132`를 그리로 옮긴다.

### F-17 · MINOR · C6 결과 화면 항목 수 (6 vs 5)

**증거**: `00-backlog.md:93`("R32 | R14 | 결과 화면 **5항목**") · `:164` · `:179` · `:125` · `:211` / 현행은 6항목 — `03-requirements.md:26` R14 · `04-features.md:23` F9 "6항목" · `02-workflow.md:27`·`:62`

**제안**: 매핑표 3곳(`00:93·164·179`)의 요약을 "결과 화면 6항목(일자 포함)"으로 고친다. 이력 문장(`:125·211`)은 당시 서술이므로 그대로 둔다.

### F-18 · MINOR · C6/C7 mermaid 노드 id 충돌

**증거**: `02-workflow.md:59` `R1["누적 거리 랭킹 반영"]` · `:60` `R2["랭킹 미반영"]` · `:62`·`:63` — 요구사항 R1(GPS 게이트)·R2(총 러닝 거리)와 표기가 같다.

**제안**: 노드 id를 `RANK_IN`·`RANK_OUT` 등으로 바꾼다.

### F-19 · MINOR · C7 저장 시점 위치 불일치

**증거**: `02-workflow.md:55` — mermaid는 SAVE 노드를 **S3 직후**에 둔다 / `02-workflow.md:26` S3 To-Be에는 저장 언급이 전혀 없고, `:27` S4 To-Be 안에 "러닝을 종료하면 … 자동 저장"이 들어 있다 / `04-features.md:20` F6 트리거는 "러닝 종료 처리 시"

**제안**: `02-workflow.md:26` S3 To-Be 끝에 "종료 처리 시 세션이 경로까지 포함해 자동 저장된다(F6)"를 넣어 표와 흐름도를 맞춘다.

### F-20 · MINOR · C3 R 근거 없는 기능 요소

**증거**: `04-features.md:15` F1 결과 "지도는 **확대·축소와 현재 위치로 재중심 이동**을 지원한다" · `02-workflow.md:24` S1에도 동일 / `03-requirements.md:32` R20에는 현재 위치·Zone 위치·Zone 범위 3가지만 있고 조작 요구가 없다.

**제안**: `03:32` R20 끝에 "지도를 확대·축소하고 현재 위치로 되돌릴 수 있어야 한다"를 추가한다.

### F-21 · MINOR · C1 근거 없는 사용처 기재

**증거**: `06-data.md:46` Ranking Zone 구역 경계 좌표의 「쓰는 기능」 = `F1, F2, F5, F9, F11` / `04-features.md:19` F5·`:23` F9·`:25` F11 결과 칸에는 Zone 경계 표시나 좌표 사용 서술이 없다(경계를 그리는 것은 F1·F2뿐이고, F9·F11의 구간 구분은 `06:33` 지점별 판정값을 쓴다).

**제안**: `06-data.md:46`의 쓰는 기능을 `F1, F2`로 줄이거나, F9·F11이 좌표를 쓰는 근거를 04에 명시한다.

### F-22 · MINOR · C2 낡은 이력 문장

**증거**: `00-backlog.md:57` — "03-requirements.md가 R1~R16(**이후 R17 추가**)으로, 04-features.md가 F1~F13→F1~F10으로 재정렬되면서" — 현행은 R23·F11까지다(`03:35`, `04:25`).

**제안**: "(이후 R17~R23 추가)", "(이후 F11 추가)"로 보강한다.

### F-23 · MINOR · C8 이력 시간순 뒤섞임

**증거**: `00-backlog.md:131`(1차) → `:132`(**3차**) → `:133`(2차) → `:134`(추가) / `00-backlog.md:258`(3차) → `:259`(2차)

**왜 문제인가**: 00-backlog 참고 이력이 "무엇이 반영돼야 했는지"의 대조 기준인데, 순서가 섞여 2차 항목이 3차로 덮였는지 판정이 어렵다.

**제안**: 각 섹션의 참고 이력을 날짜·라운드 오름차순으로 재배열한다.

### F-24 · MINOR · C4 복원 항목이 제거 표에 잔류

**증거**: `00-backlog.md:42`·`:117`·`:201` — 세 행 모두 "**전체 복원(2026-09-11 2차)**"이면서 「제거·후순위 전환된 항목」 표 안에 남아 있다.

**제안**: 세 행을 「참고 이력」의 복원 기록으로 옮기거나, 표 상단에 "전체 복원 표기가 있는 행은 현재 MVP 항목"이라는 한 줄을 둔다.

### F-25 · MINOR · C6 용어 드리프트

**증거**: `01-problem.md:33` "탄천 **지역 랭킹 거리**" · `:35·36·56·78` "탄천 **구역**" · `:50` "탄천 **지정 구역**(Ranking Zone)" · `:56` "탄천 **랭킹 점수**"(어느 문서에도 점수 개념 없음) / `04-features.md:23` F9 입력 "탄천 **구역** 내 누적 거리" / 02~06 표준어는 "탄천 Ranking Zone" · "탄천 인정 거리"

**제안**: `01:33·35·36·56·78`과 `04:23`을 "탄천 Ranking Zone" · "탄천 인정 거리"로 통일하고, `01:56`의 "랭킹 점수"를 "누적 거리"로 바꾼다.

---

## 확인 필요

- 3차 UID를 03에 R로 세우지 않은 판단: 저장 구조 결정이라는 근거는 타당하나, "재로그인해도 기록·랭킹이 이어진다"는 사용자가 체감하는 요구다. R로 세울 것인가, 04 F8 확장으로만 처리할 것인가?
- P9의 속도 초과 제외는 R12의 "Zone 외부 거리도 개인 기록에 포함" 규칙보다 우선하는가? (우선한다면 R12에 예외 문구가 필요하다.)
- 01의 「MVP 핵심 워크플로우」는 S0~S8 전체를 담아야 하는가, 3단계 요약으로 유지하는가?
- F8(소셜 로그인)에 대응 정책이 정말 불필요한가? (인증 실패·동일 소셜 계정 재가입 처리 기준이 어느 문서 소관인가)
- 06의 판정 대기 `[제안]` 5건(가입 일시·표시 순위·PR 갱신 여부·최고 기록 일자·GPS 신호 상태)은 언제 채택/기각 판정을 내리는가? (미채택이므로 이번 감사에서 누락으로 세지 않았다.)

## product-planner 영역

- `03:22` R10 "최장 거리·최고 페이스·최대 운동 시간 **등**" — 항목이 열려 있다.
- `04:15` F1 "GPS 확인이 완료되면" — 완료 판정 기준이 없다.
- `06:32` GPS 저장 주기 `[?]`, `06:46` 좌표 형태·정밀도 `[?]`.
- `05:17` P5 "동일 거리인 경우 먼저 달성한 사용자가 상위" — 달성 시각의 기준(측정 시각/종료 타임스탬프)이 모호.
- `04:23` F9 "PR이 갱신되었다면 이 화면에서 즉시 안내한다" — 안내 형태가 비어 있다.

## 추적성 요약

끊긴 연결만:

- `R→F` : **R11**(Zone 내부만 랭킹 반영) · **R12**(Zone 외부도 개인 기록) — 04의 R 열 부재, 산문에만 등장
- `F→P` : **F8**(소셜 로그인) — 05의 어떤 P에도 대상 F로 없음
- `F→R` : 04 F1의 "지도 확대·축소·재중심" — 03에 대응 R 없음
- `05→04` : 04 「정책(P)」 열 전체 공란(지시에 따라 보고 제외) — 이 방향은 현재 검증 불가
- `S→R` / `A→R·F` : 끊김 없음(S0~S8 전부 03의 출처 열에 등장, A1은 모든 R·F에 배정)
- `F11→06` : 끊김 없음 — F11이 표시하는 6항목·구간 구분이 `06:18·21·22·23·24·25·29~34`로 전부 조회 가능

## 재발 방지 권고

1. **머리말 규칙을 먼저 갱신하라.** 번호 정책이 2차에 바뀌었는데 03·04·05 머리말은 옛 규칙 그대로다(F-11). 정책 변경 커밋에 해당 문서 머리말 갱신을 포함시키면 F-02 유형의 재발이 차단된다.
2. **백로그의 옛 번호는 예외 없이 `구` 접두어를 붙여라.** "현재 번호와 안 겹치는 범위"라는 조건부 규약은 신규 번호가 뒤에 붙는 순간 무너진다(F-02). 접두어를 무조건화하는 것이 유일하게 안전하다.
3. **00-backlog 참고 이력에 「반영 대상 문서」 칸을 두라.** 이번 3차 P9(`00:258`)는 03을, 닉네임 제한(`00:134`)은 02를 목록에서 빠뜨렸고 그대로 전파 누락이 됐다(F-01·F-07). 한 커밋이 한 문서만 건드리는 팀 구조에서는 이력이 유일한 전파 체크리스트다.

SELF-AUDIT: PASS
