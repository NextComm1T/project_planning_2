# 주요 기능 — 탄천 로컬 개인 랭킹전

> 문서: 04-features.md · 상태: 초안(우선순위·인수 조건 미확정) · 열린 질문 [?] 0개 · 채택한 [제안] 3개(F5·F7·F8 — 그 외 검토된 제안은 후순위 전환되어 00-backlog.md 참고) · 마지막 갱신: 2026-09-11
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.
> 번호 규칙: 제거·후순위 항목은 00-backlog.md로 옮기되 **남은 항목의 번호는 재정렬하지 않고 신규는 뒤에 붙인다**(2026-09-11 2차 변경). 이전 번호 ↔ 현재 번호 매핑은 00-backlog.md 참고.
> [제안] 표기 규칙: 채택된 [제안]은 태그를 지우고 확정 문장으로만 남긴다 — 본문에 [제안] 표기가 없는 것이 정상이며, 무엇이 채택된 제안이었는지는 위 머리말의 열거와 00-backlog.md 참고 이력으로 확인한다.
> 트리거·입력·결과·예외 열의 "(출처: ...)"는 02-workflow.md의 To-Be 칸 또는 03 인터뷰 때 팀이 준 "02-workflow.md에서 분리한 상세 정책" 자료(랭킹 정책·개인 기록 정책·GPS 정책·러닝 화면 요구사항·러닝 종료 정책·프로필 정책)에서 그대로 가져왔음을 표시한다.
> 정책(P) 열은 05-policy.md 「대상 F」 열을 역으로 모아 채웠다(2026-09-11 9차). 우선순위 2축 표와 인수 조건 열은 팀 결정 후에 채운다 — 화면 9개 전부에 똑같이 적용하는 완료 기준은 07-screens.md 「모든 화면 공통 인수 조건」에 있다.

---

## 기능

| F | R | 행위자 | 트리거 | 입력 | 결과(보이는 것) | 예외(보이는 것) | 정책(P) | 우선순위 | 인수 조건 |
| - | - | ------ | ------ | ---- | -------------- | -------------- | ------- | -------- | -------- |
| F1 | R1, R16, R20 | A1 | 러닝 시작 화면 진입, 백그라운드에서 GPS 상태 확인 (출처: GPS 정책 · S1) | 위치 권한 허용 여부, GPS 상태, 현재 위치, 탄천 Ranking Zone 좌표·범위(시스템에 사전 설정), GPS 활성화·확인이 완료되어 활성화된 시작 버튼을 클릭 | GPS 확인이 완료되면 시작 버튼이 활성화되고, 지도에 **현재 위치**와 **탄천 Ranking Zone의 위치·범위**가 함께 표시된다. 지도는 확대·축소와 현재 위치로 재중심 이동을 지원한다. 시작 버튼을 클릭하면 3초 카운트다운(사용자가 준비할 시간을 주는 용도일 뿐, GPS 확인과는 무관) 후 러닝 화면(F2)으로 전환된다 — **세션이 "진행 중"이 되고 러닝 시간을 재기 시작하는 시점은 카운트다운이 끝나는 순간이다**(05 상태값 · R16). **카운트다운 도중에는 취소할 수 있고, 취소하면 이 화면에 그대로 남아 세션은 "시작 전"을 유지한다** | GPS가 비활성 상태이거나 확인이 끝나기 전, 또는 현재 위치를 확인할 수 없는 동안에는 시작 버튼이 비활성화된 채 "GPS 신호를 확인할 수 없어 러닝을 시작할 수 없습니다" 안내 표시, 지도의 현재 위치 표시는 준비 상태로 유지된다. 버튼을 누를 수 있다는 것 자체가 GPS 정상이라는 뜻이므로, 네트워크가 불안정해 확인이 늦어져도 잘못 시작되지 않는다. **위치 권한을 허용하지 않은 경우에도 시작 버튼은 비활성 상태이며, "위치 권한을 허용해야 러닝을 시작할 수 있습니다" 안내와 권한 설정으로 가는 길을 함께 보여 준다** · P1. 다른 기기를 포함해 이미 "진행 중" 세션이 있으면 "이미 진행 중인 러닝이 있습니다" 안내와 함께 시작을 막는다 · P7 | P1, P7 |  |  |
| F2 | R2, R3, R4, R6, R17, R18, R19 | A1 | 러닝 진행 화면에서 실시간 (출처: 러닝 화면 요구사항 · S2) | 내부 GPS 위치·타이머 데이터, 탄천 Ranking Zone 좌표(시스템에 사전 설정되어 있음 — 설정 주체는 이 문서 범위 밖) | 화면에 총 러닝 거리·탄천 인정 누적 거리·러닝 시간·현재 페이스(속력 — 최근 15초 기준, 저장하지 않는 계산값) 4항목이 실시간으로 갱신되어 표시되고, **지도에 현재 위치가 실시간으로 갱신되며 탄천 Ranking Zone 경계와 이번 러닝에서 지나온 경로가 함께 표시**되며, **러닝 종료 버튼이 상시 함께 표시**된다(누르면 F9 결과 화면으로 이어짐). 순위는 이 화면에 표시하지 않는다(러닝 종료 후 F9·F5에서만 확인 가능) | GPS 신호가 끊긴 구간에서는 거리·탄천 인정 거리 값이 직전 측정값에 고정되고, 지도상 위치 핀도 직전 위치에 멈추며 경로 선이 이어지지 않는다(신호가 복구되면 그 지점부터 다시 이어짐 — F4와 동일 규칙). 시속 25km를 넘는 비정상 이동 구간은 Zone 안팎을 가리지 않고 총 러닝 거리·탄천 인정 거리 두 값 모두에서 조용히 빠진다 · P9. Zone 밖에서 달리는 동안은 탄천 인정 거리가 증가하지 않고 유지됨. **화면이 꺼지거나 다른 앱으로 전환된 동안(앱이 닫힌 것은 아닌 상태)에도 측정은 그대로 계속되고 그 구간도 거리·경로에 포함한다.** 앱이 완전히 종료됐다가 다시 열렸을 때 진행 중 세션이 있으면 이 화면으로 돌아와 이어서 측정하며, **앱이 완전히 닫혀 있던 동안만** GPS 신호가 끊긴 구간과 같이 거리·경로에서 제외한다 · P2. **인터넷 연결이 끊겨도 측정은 멈추지 않고 계속되며, 랭킹 반영만 연결이 복구된 뒤로 미뤄진다** · P14. 러닝 시작 후 15초가 차기 전과, 최근 15초 동안 이동 거리가 10m 미만이면 현재 페이스 칸에 숫자 대신 "—"를 표시한다 | P2, P9, P14 |  |  |
| F3 | R5 | A1 | 러닝 중 GPS 신호 끊김 상태가 2초 이상 지속 (출처: 러닝 화면 요구사항 · GPS 정책) | 내부 GPS 신호 상태 | 화면에 GPS 경고 아이콘과 "GPS 신호가 끊겼습니다" 문구 표시 | 신호가 다시 정상으로 돌아오면 경고가 사라짐. 이 2초는 **경고를 띄우는 기준일 뿐**이며, 거리·경로 제외는 신호가 끊긴 즉시 적용된다 · P2 | P3 |  |  |
| F4 | R6 | A1 | 러닝 중 GPS 신호 끊김 발생 → 복구 (출처: GPS 정책) | 내부 GPS 신호 상태(끊김·복구) | 끊긴 구간은 거리 계산에서 제외되고 복구된 지점부터 다시 측정 시작. 제외는 신호를 잃은 **즉시** 적용되며 F3·P3 의 2초 경고 기준과 무관하다 · P2 | 해당 없음 | P2 |  |  |
| F5 | R7 | A1 | 랭킹 화면 진입 (출처: 랭킹 정책) | 없음(조회) | 탄천 Ranking Zone 누적 거리 기준 전체 순위 리스트 표시, 각 순위마다 닉네임과 누적 거리 표시. 이번 러닝에서 탄천 인정 거리가 발생했다면 러닝 종료 처리(F6)에서 계산된 순위가 여기 반영되어 있다. **리스트에는 누적 탄천 인정 거리가 0보다 큰 사용자만 올리고 인원 제한 없이 전체를 표시하며**(페이징은 후순위 — 00-backlog.md 참고), **목록에서 본인 줄을 강조해 보여 준다**(본인 순위를 따로 요약해 두지는 않는다) · P5 | 랭킹 데이터가 아직 없는 경우 "아직 랭킹 데이터가 없습니다" 문구 표시 | P5, P8 |  |  |
| F6 | R6, R8, R12 | A1 | 러닝 종료 처리 시 자동 실행 + 사용자가 기록 탭 진입 시 조회 (출처: 개인 기록 정책 · S6) | 이번 러닝의 세션 데이터(일자·총 러닝 거리·탄천 인정 거리·러닝 시간·평균 페이스·GPS 이동 경로·Ranking Zone 인정 구간 정보) + 랭킹 집계의 누적 거리(순위 계산용)와 기존 개인 최고 기록(갱신 판정용) | 이번 러닝 기록이 Zone 안팎 관계없이 일자별로 자동 저장된다 — 총 러닝 거리에는 Zone 외부 거리도 포함하고(R12), 탄천 인정 거리에는 Zone 내부 인정 거리만 포함하며, **GPS 이동 경로와 Ranking Zone 인정 구간 정보도 함께 저장한다.** **개인 순위(누적 거리 기준)와 개인 최고 기록 갱신 여부를 이때 계산해 세션에 함께 저장한다** — 두 값을 만드는 주체는 F6이고, F9는 저장된 값을 결과 화면에 표시하기만 한다. 저장된 값은 F7(기록 탭)의 목록·개인 기록 요약과 F11(기록 상세)에서 사용되고, 개인 최고 기록과 개인 누적 기록이 갱신된다. 개인 최고 기록은 세션 목록과 별개로 갱신·보관된다 | Zone 밖에서만 달린 경우에도 세션은 저장되며, 그 세션의 탄천 인정 거리는 0으로 저장된다. GPS 신호가 끊긴 구간은 거리와 경로 어느 쪽에도 포함하지 않고, 신호가 복구된 지점부터 다시 기록한다 · P2. 시속 25km를 넘는 비정상 이동 구간은 Zone 안팎을 가리지 않고 총 러닝 거리·탄천 인정 거리 두 값 모두에서 빼지만, 측정 지점과 경로 자체는 제외 사유와 함께 저장한다 · P9. **저장·순위 계산이 실패하면 결과 화면에 오류 상태와 "다시 시도"를 보여 주고, 저장이 성공하기 전까지 그 세션을 기록 탭(F7)·랭킹(F5)에 넣지 않는다** · P14 · 07 공통 인수 조건 ①. 총 러닝 거리가 0인 세션도 그대로 저장·표시하지만, 거리가 0이면 평균 페이스를 낼 수 없으므로 **개인 최고 기록의 최고 페이스 갱신 판정에서는 제외한다**(최장 거리·최대 운동 시간 판정은 그대로 한다) | P2, P6, P9, P14 |  |  |
| F7 | R9, R10, R23 | A1 | 기록 탭 진입 | 없음 (정렬·필터 옵션은 MVP 범위 밖 — 00-backlog.md 참고) | 기록 탭 상단에 본인 닉네임과 함께 ① **전체 러닝 총 누적 거리**와 **탄천 Ranking Zone 인정 누적 거리**를 각각 표시하고, 그 아래에 ② 러닝을 거듭할수록 동적으로 갱신되는 개인 최고 기록(최장 거리·최고 페이스·최대 운동 시간)과 ③ 과거 러닝 세션 목록(일자·총 러닝 거리·탄천 인정 거리·시간·평균 페이스)을 함께 표시한다 — **목록은 최신 러닝이 맨 위에 오는 순서이고 보이는 개수에 제한이 없다**(정렬·필터 옵션과 페이징은 MVP 범위 밖). 목록에서 특정 세션을 선택하면 F11(기록 상세) 화면으로 이동한다 | 기록이 없는 경우(신규 가입 직후) "아직 기록이 없습니다" 등 빈 상태 화면 표시, 누적 거리 두 값은 0으로 표시. 첫 러닝은 그 자체가 개인 최고 기록으로 표시됨 | P6 |  |  |
| F8 | R13, R24 | A1 | ① 앱 최초 실행 후 로그인 화면 진입(회원가입) ② 로그아웃 상태에서 앱 실행 후 로그인 화면 진입(재로그인) (출처: S0) | 카카오 또는 구글 계정 선택 및 인증 동의 | **최초 가입 시** — 소셜 로그인 인증에 성공하면 사용자 UID가 발급되고 닉네임 설정 단계(F10)로 이동한다. **재로그인 시** — 인증에 성공하면 소셜 계정 식별자로 기존 계정을 찾아 새 계정을 만들지 않고 그 계정으로 로그인되며, 닉네임·러닝 기록·랭킹 누적이 그대로 보인다(닉네임 설정 단계를 다시 거치지 않는다). 로그인한 뒤에는 로그아웃(F12)하기 전까지 앱을 다시 열어도 로그인 상태가 유지된다. **한 계정으로 여러 기기에 동시에 로그인할 수 있고, 카카오와 구글은 소셜 계정 식별자가 다르므로 같은 사람이라도 서로 다른 계정 2개가 된다** · P11. **재로그인한 계정에 "진행 중" 세션이 남아 있으면 러닝 진행 화면(F2)으로 돌아가 이어서 측정한다** · P7·P2 | 인증 실패 또는 취소 시 로그인 실패 안내 표시, 회원가입·로그인 진행되지 않음. 최초 가입 중 닉네임 설정을 마치지 않고 앱을 닫았다면 다음 앱 실행 또는 로그인 때 닉네임 설정 단계(F10)부터 다시 진행한다 · P11 | P11 |  |  |
| F9 | R14, R11 | A1 | F2(러닝 진행 화면)의 러닝 종료 버튼 클릭(S3→S4) | 세션 전체 GPS 데이터(총 거리·탄천 Ranking Zone 인정 거리), 세션 타이머 값, 결과 화면에 표시할 개인 순위와 개인 최고 기록 갱신 여부(둘 다 F6이 계산·저장한 값), 세션 총 거리·시간으로 계산한 속력값, 세션 일자 | 결과 화면에 이번 러닝의 6항목을 모두 표시 — ① 총 러닝 거리(0인 경우도 특수 처리 없이 0으로 정상 표시) ② 탄천 인정 거리(0이면 0으로 표시, R11 반영 규칙에 따름) ③ 개인 순위(누적 거리 기준) ④ 러닝 시간 ⑤ 평균 페이스(**분/km** — 세션 총 거리·시간으로 계산한 단순 값. 거리·시간 등 예외 조건 고려 없이 표시하되 **총 러닝 거리가 0이면 숫자 대신 "—"를 보인다**. 러닝 중 화면의 현재 페이스와 다르다) ⑥ 일자. 6항목과 별개로 본인 닉네임을 화면에 함께 표시한다(F10 수정 반영 대상). 여기에 이번 러닝의 GPS 이동 경로가 그려진 지도를 함께 표시하며, 지도에서는 **Ranking Zone 인정 구간**과 **일반 러닝 구간**을 서로 다른 스타일로 구분한다(시속 25km 초과로 거리에서 뺀 구간은 별도 스타일 없이 일반 러닝 구간과 같게 그린다 · P9). 러닝 종료 처리 시 F6에 의해 세션이 경로까지 포함해 자동 저장되고, 탄천 인정 거리가 있으면 F5의 랭킹에도 반영된다. 이번 러닝으로 개인 최고 기록(PR)이 갱신되었다면 이 화면에서 즉시 **배지와 문구로** 안내한다(예: "최장 거리 갱신" — 갱신된 항목 이름을 그대로 보여 주며, 기록의 실제 값은 F7 기록 탭에서 확인) | 랭킹 데이터가 아직 없는 경우(가입 초기 등) 순위 항목은 "아직 랭킹 데이터가 없습니다" 문구로 표시하고, 랭킹 데이터가 있는데 이번 세션의 탄천 인정 거리가 0이면 "랭킹 미반영" 문구로 표시한다 — **두 경우가 동시에 해당하면 "아직 랭킹 데이터가 없습니다"가 우선한다**. 러닝 종료 처리(자동 저장·순위 계산)가 실패하면 이 화면에 오류 상태와 "다시 시도"를 보인다 · P14 · 07 공통 인수 조건 ① | P2, P4, P8, P9, P14 |  |  |
| F10 | R15, R22 | A1 | ① 최초 소셜 로그인 완료 후 닉네임 설정 화면 진입(F8 직후) ② 가입 완료 후 설정 화면에서 닉네임 수정 기능 선택 | 닉네임 직접 입력 | **최초 가입 시** — 유효한 닉네임을 저장하고, 설정을 마치면 서비스 이용을 계속한다. **가입 후 수정 시** — 설정 화면에 현재 닉네임을 표시하고, 사용자가 새 닉네임을 입력해 수정할 수 있다. 수정이 성공하면 저장된 닉네임을 새 값으로 수정하고, 수정된 닉네임을 이후 랭킹 화면(F5)·개인 기록 화면(F7·F9) 등 본인의 닉네임을 표시하는 모든 화면에 반영한다 | 미입력 / 9자 초과 / 한글·영문·숫자 이외의 문자 포함 / 공백 포함 / 다른 사용자가 이미 사용 중인 닉네임이면 저장·수정하지 않고 **사유별로 다른 문구로** 재입력을 안내한다 — 미입력은 "닉네임을 입력해 주세요", 형식 위반(9자 초과·공백·특수문자)은 "닉네임은 9자 이하의 한글·영문·숫자만 쓸 수 있습니다", 중복은 "이미 사용 중인 닉네임입니다"(P10). 중복 판정에서 영문 대소문자는 구분하지 않고, 한글 자음·모음만으로 된 닉네임은 허용한다 · P10. 최초 가입 시에는 다음 단계로 진행할 수 없고, 수정 시에는 **현재 닉네임을 그대로 유지**한다 | P10, P11 |  |  |
| F11 | R21 | A1 | F7(기록 탭)의 과거 러닝 세션 목록에서 특정 세션 선택 | 선택한 러닝 세션의 저장 데이터(F6이 저장한 값) | 기록 상세 화면에 ① 일자 ② 총 러닝 거리 ③ 탄천 인정 거리 ④ 러닝 시간 ⑤ 평균 페이스 ⑥ 전체 GPS 이동 경로 지도를 표시한다. 지도에서는 **Ranking Zone 인정 구간**과 **일반 러닝 구간**을 서로 다른 스타일로 표시한다(시속 25km 초과로 거리에서 뺀 구간은 별도 스타일 없이 일반 러닝 구간과 같게 그린다 · P9) | GPS 신호가 끊겼던 구간은 직선으로 연결하지 않고 임의 보간도 하지 않으며, 실제 저장된 유효 GPS 경로만 표시한다 · P2 | P2, P6 |  |  |
| F12 | R25 | A1 | 설정 화면에서 로그아웃 선택 (출처: S8) | 없음 | 로그아웃 상태가 되어 로그인 화면(F8)으로 돌아간다. 저장된 계정·러닝 기록은 그대로 남고, 같은 소셜 계정으로 다시 로그인하면 이어진다(F8 ②) | 진행 중인 러닝 세션이 있으면 "진행 중인 러닝을 먼저 종료해 주세요" 안내 후 로그아웃하지 않는다 · P12 | P12 |  |  |
| F13 | R26 | A1 | 설정 화면에서 회원 탈퇴 선택 (출처: S8) | 탈퇴 확인(삭제되는 항목 안내를 보고 한 번 더 확인) | 확인하면 즉시 탈퇴 처리된다 — 계정(「사용자」 항목 전부 — 06 비기능 절 「회원 탈퇴(F13) 시 삭제 범위」 참고)과 본인의 러닝 세션·GPS 측정 지점·개인 최고 기록·랭킹 집계가 모두 삭제되어 랭킹 화면(F5)에서도 빠지고, 로그인 화면(F8)으로 돌아간다. 쓰던 닉네임은 해제되어 다른 사용자가 쓸 수 있다 · P13 | 확인 단계에서 취소하면 아무것도 삭제하지 않는다. 진행 중인 러닝 세션이 있으면 "진행 중인 러닝을 먼저 종료해 주세요" 안내 후 탈퇴하지 않는다 · P12. 같은 소셜 계정으로 다시 가입하면 새 계정으로 시작하며 이전 기록은 복구되지 않는다 · P13 | P12, P13 |  |  |

> 검토했으나 제외한 후보, 문서 정리 이력, 이전 본문 주석은 [00-backlog.md](00-backlog.md)에 모아뒀다.

## 우선순위 2축

| F | 중요도(팀) | 노력(코치) | MVP / v2 | 이유 |
| - | ---------- | ---------- | -------- | ---- |
