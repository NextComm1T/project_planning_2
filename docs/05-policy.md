# 정책 정의서 — 탄천 로컬 개인 랭킹전

> 문서: 05-policy.md · 상태: 초안 · 열린 질문 [?] 0개 · 채택한 [제안] 4개(P9·P10·P11·SP4 — SP4는 P9로 통합) · 마지막 갱신: 2026-09-09
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.
> P8·SP5는 02-workflow.md의 A2(관리진) 제거로 무효화되어 번호만 유지하고 내용을 비웠다(아래 각주 참고).

---

## 정책

| P | 대상 F | 누가 | 언제(상태값) | 규칙 (할 수 없다 · 까지) | 위반 시 앱이 보이는 것 |
| - | ------ | ---- | ------------ | ----------------------- | --------------------- |
| P1 | F1, F2, F4 | 본인(A1) | 프로필 설정·변경 시 상시 | 목표 거리는 1km~100km, 목표 시간은 5분~360분(6시간) 범위 안에서만 설정·변경할 수 있다 | 범위 밖 값은 저장을 거부하고 안내 문구 표시 |
| P2 | F5 | 본인(A1) | 러닝 세션 상태 = "시작 전" | GPS 신호가 정상이어야만 러닝을 시작할 수 있다 | "GPS 신호를 확인할 수 없어 러닝을 시작할 수 없습니다" 안내, 러닝 시작 차단 |
| P3 | F6, F8, F11 | 본인(A1) | 러닝 세션 상태 = "진행 중" | GPS 신호가 끊긴 구간은 거리·탄천 인정 거리 계산에 포함할 수 없다 | 끊긴 구간은 직전 측정값을 고정 표시, 신호가 복구되면 그 지점부터 다시 측정 |
| P4 | F9 | 본인(A1) | 러닝 세션 상태 = "진행 중" | GPS 신호 없음 상태가 2초 이상 지속되면 경고를 표시해야 한다 | 화면에 GPS 상태 경고 표시 |
| P5 | F12, F13, F14, F15, F16, F27 | 본인(A1) | 러닝 세션 상태가 "종료"로 전이된 이후 | 본인 세션의 결과만 조회할 수 있고, 세션이 "종료" 상태가 되기 전에는 결과 화면에 접근할 수 없다 | 해당 없음 (종료 전에는 결과 화면 진입 트리거 자체가 없음) |
| P6 | F17, F18, F19, F20 | 모든 A1 | 랭킹 집계 상태 = "집계 중"(주간·월간 각각) | 랭킹은 탄천 Ranking Zone 내부 누적 거리 기준으로만 산정한다. 동일 거리인 경우 해당 거리를 먼저 달성한 사용자가 상위다. TOP 100 안에 든 사용자는 하단에 별도 표시하지 않는다. 다른 사용자의 프로필·상세 정보는 볼 수 없고 닉네임과 누적 거리만 노출한다 (닉네임 부여 규칙은 R33·F30 참고 — 가입 시 직접 설정, 필수) | 해당 없음 (다른 사용자 프로필 조회 기능 자체가 없음 — 01 MVP 제약) |
| P7 | F21, F24, F26 | 본인(A1) | 상시 (Ranking Zone 안팎 관계없이) | 본인의 개인 기록만 조회할 수 있고, 누적 저장 자체에는 한도가 없다 | 해당 없음 |
| P8 | (해당 없음 — 무효화됨) | — | — | **무효화됨** — 02-workflow.md에서 A2(관리진)가 팀 결정으로 제거되어 이 정책의 전제(A2만 설정 가능)가 사라졌다. Ranking Zone 좌표 설정 주체는 이 문서 범위 밖으로 둔다 | 해당 없음 |
| P9 | F5 | 본인(A1) | 이미 "진행 중" 상태인 세션이 있는 상태에서 새 러닝을 시작하려 할 때 | 한 계정은 동시에 하나의 진행 중인 러닝 세션만 가질 수 있다 | "이미 진행 중인 러닝이 있습니다" 안내, 새 러닝 시작 차단 |
| P10 | F15, F16, F17, F18, F19, F20 | 시스템(자동) | 두 사용자의 러닝 종료 처리가 동시에 들어올 때 | 서버는 세션 종료 시각(타임스탬프) 순서대로 순차 처리하여 순위를 갱신해야 한다 | 해당 없음(사용자에게 노출되지 않는 내부 처리 규칙) |
| P11 | F8, F13 | 시스템(자동) | 러닝 세션 상태 = "진행 중", Ranking Zone 내부 이동 중 | 비정상적으로 빠른 이동 속도(예: 시속 25km 초과) 구간은 탄천 인정 거리 계산에서 제외해야 한다 | 해당 구간만 조용히 제외, 별도 경고 없음 |

## 상태값

- **러닝 세션**: 시작 전 → 진행 중 → 종료 · 전이 조건: "러닝 시작" 버튼 클릭 + GPS 정상 확인 시 시작 전→진행 중, 사용자가 "종료" 버튼을 직접 클릭 시 진행 중→종료. 목표 미달성 상태로 종료하면 확인 메시지(F29)를 거쳐 종료 처리됨 (자동 종료·일시정지는 MVP 미지원 — 02-workflow.md 「검토했으나 제외」 표 참고)
- **GPS 신호**: 정상 → 끊김 → 복구(정상) · 전이 조건: 신호 유실 시 정상→끊김, 신호 재획득 시 끊김→복구(정상 상태로 합류)
- **주간 랭킹 집계**: 집계 중 → 마감 → 새 주 집계 시작 · 전이 조건: 매주 월요일 00:00 도달 시 자동으로 마감 후 새 주 집계 시작 (팀 원문 확정)
- **월간 랭킹 집계**: 집계 중 → 마감 → 새 달 집계 시작 · 전이 조건: 매월 1일 00:00 도달 시 자동으로 마감 후 새 달 집계 시작 (팀 원문 확정)

## 서비스 정책

| SP | 한 줄 |
| -- | ----- |
| SP1 | 랭킹 초기화와 개인 기록 보존은 별개다 — 주간·월간 랭킹이 초기화되어도 개인의 전체 러닝 기록은 삭제되지 않고 계속 유지된다. (팀 원문 확정) |
| SP2 | 서비스는 다른 사용자의 프로필이나 상세 정보를 조회하는 기능을 제공하지 않는다. (01-problem.md MVP 개발 제약 사항 재확인) |
| SP3 | 서비스는 크루·팀 단위 기능을 제공하지 않으며, 랭킹은 오직 개인 단위로만 존재한다. (01-problem.md MVP 개발 제약 사항 재확인) |
| SP4 | **P9로 통합됨** — 아래 「검토했으나 제외」 표 참고 |
| SP5 | **무효화됨** — 02-workflow.md에서 A2(관리진)가 팀 결정으로 제거되어 "Ranking Zone 좌표는 A2만 설정 가능"이라는 전제가 사라졌다. Ranking Zone 좌표 설정 주체는 이 문서 범위 밖으로 둔다 |

> 주: P8·SP5는 번호를 유지한 채 내용만 비웠다(번호 재정렬 금지 원칙). 팀이 A2를 다시 살리기로 하면 이 두 행을 원래 내용(A2만 설정 가능)으로 되돌리면 된다.
> 주: P7의 대상 F에서 F22·F23·F25가 빠졌다 — 지난주·지난달 기록 비교(F22·F23)와 러닝 경로 지도(F25)가 팀 결정으로 후순위 전환되었기 때문이다(03-requirements.md·04-features.md 「검토했으나 제외」 표 참고).

## 검토했으나 제외

| 후보 | 출처(누구 초안 · [제안]) | 제외 이유 |
| ---- | ------------------------ | --------- |
| SP4(동시 세션 제한) | 자체 제안 | P9와 내용이 완전히 중복되어 별도 유지하지 않고 P9로 통합 |

