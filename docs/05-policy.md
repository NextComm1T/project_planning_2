# 정책 정의서 — 탄천 로컬 개인 랭킹전

> 문서: 05-policy.md · 상태: 초안 · 열린 질문 [?] 0개 · 채택한 [제안] 3개(P7·P8·P9 — 그 외 검토된 제안은 후순위/무효화되어 00-backlog.md 참고) · 마지막 갱신: 2026-09-11
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.
> 번호 규칙: 2026-09-10부터 제거·무효화·후순위 전환된 항목은 본문에 남기지 않고 00-backlog.md로 옮기며, 남은 항목은 빈 번호 없이 재정렬한다. 이전 번호 ↔ 현재 번호 매핑은 00-backlog.md 참고.

---

## 정책

| P | 대상 F | 누가 | 언제(상태값) | 규칙 (할 수 없다 · 까지) | 위반 시 앱이 보이는 것 |
| - | ------ | ---- | ------------ | ----------------------- | --------------------- |
| P1 | F1 | 본인(A1) | 러닝 세션 상태 = "시작 전" | GPS 활성화·확인이 완료되기 전에는 러닝 시작 버튼을 활성화할 수 없고, 러닝도 시작할 수 없다 | 시작 버튼 비활성화 + "GPS 신호를 확인할 수 없어 러닝을 시작할 수 없습니다" 안내 |
| P2 | F2, F4 | 본인(A1) | 러닝 세션 상태 = "진행 중" | GPS 신호가 끊긴 구간은 거리·탄천 인정 거리 계산에 포함할 수 없다 | 끊긴 구간은 직전 측정값을 고정 표시, 신호가 복구되면 그 지점부터 다시 측정 |
| P3 | F3 | 본인(A1) | 러닝 세션 상태 = "진행 중" | GPS 신호 없음 상태가 2초 이상 지속되면 경고를 표시해야 한다 | 화면에 GPS 상태 경고 표시 |
| P4 | F9 | 본인(A1) | 러닝 세션 상태가 "종료"로 전이된 이후 | 본인 세션의 결과만 조회할 수 있고, 세션이 "종료" 상태가 되기 전에는 결과 화면에 접근할 수 없다 | 해당 없음 (종료 전에는 결과 화면 진입 트리거 자체가 없음) |
| P5 | F5 | 모든 A1 | 랭킹 집계 상태 = "집계 중" | 랭킹은 탄천 Ranking Zone 내부 누적 거리 기준으로만 산정한다. 동일 거리인 경우 해당 거리를 먼저 달성한 사용자가 상위다. 다른 사용자의 프로필·상세 정보는 볼 수 없고 닉네임과 누적 거리만 노출한다 (닉네임 부여 규칙은 R15·F10 참고 — 가입 시 직접 설정, 필수) | 해당 없음 (다른 사용자 프로필 조회 기능 자체가 없음 — 01 MVP 제약) |
| P6 | F6, F7 | 본인(A1) | 상시 (Ranking Zone 안팎 관계없이) | 본인의 개인 기록만 조회할 수 있고, 누적 저장 자체에는 한도가 없다 | 해당 없음 |
| P7 | F1 | 본인(A1) | 이미 "진행 중" 상태인 세션이 있는 상태에서 새 러닝을 시작하려 할 때 | 한 계정은 동시에 하나의 진행 중인 러닝 세션만 가질 수 있다 | "이미 진행 중인 러닝이 있습니다" 안내, 새 러닝 시작 차단 |
| P8 | F5, F9 | 시스템(자동) | 두 사용자의 러닝 종료 처리가 동시에 들어올 때 | 서버는 세션 종료 시각(타임스탬프) 순서대로 순차 처리하여 순위를 갱신해야 한다 | 해당 없음(사용자에게 노출되지 않는 내부 처리 규칙) |
| P9 | F2, F9 | 시스템(자동) | 러닝 세션 상태 = "진행 중", Ranking Zone 내부 이동 중 | 비정상적으로 빠른 이동 속도(예: 시속 25km 초과) 구간은 탄천 인정 거리 계산에서 제외해야 한다 | 해당 구간만 조용히 제외, 별도 경고 없음 |

## 상태값

- **러닝 세션**: 시작 전 → 진행 중 → 종료 · 전이 조건: "러닝 시작" 버튼 클릭 + GPS 정상 확인 시 시작 전→진행 중, 사용자가 "종료" 버튼을 직접 클릭 시 진행 중→종료 (자동 종료·일시정지는 MVP 미지원 — 00-backlog.md 참고)
- **GPS 신호**: 정상 → 끊김 → 복구(정상) · 전이 조건: 신호 유실 시 정상→끊김, 신호 재획득 시 끊김→복구(정상 상태로 합류)
- **랭킹 집계**: 집계 중 (단일 상태, 초기화 없이 누적) — 주간·월간 구분이 후순위로 빠지면서 별도 마감·초기화 전이 없이 계속 누적되는 단일 집계로 단순화됨

## 서비스 정책

| SP | 한 줄 |
| -- | ----- |
| SP1 | 서비스는 다른 사용자의 프로필이나 상세 정보를 조회하는 기능을 제공하지 않는다. 러닝 중·결과 화면 지도(F2·F9)에도 본인의 위치·경로만 표시하며, 다른 사용자의 실시간 위치는 노출하지 않는다. (01-problem.md MVP 개발 제약 사항 재확인) |
| SP2 | 서비스는 크루·팀 단위 기능을 제공하지 않으며, 랭킹은 오직 개인 단위로만 존재한다. (01-problem.md MVP 개발 제약 사항 재확인) |

> 검토했으나 제외한 후보, 문서 정리 이력, 이전 본문 주석은 [00-backlog.md](00-backlog.md)에 모아뒀다.
