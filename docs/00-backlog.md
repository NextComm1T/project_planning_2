# 후순위 백로그 — 탄천 로컬 개인 랭킹전

> 문서: 00-backlog.md · 목적: 01~07 · PRD 문서에서 제거되었거나 후순위로 전환된 항목을 한곳에 모아 각 문서 본문(번호 목록·검토했으나 제외 표)을 간결하게 유지한다.
> 원래 R/F/P 번호가 있던 항목은 "원 번호" 열에 그 번호를 남기지만, 이 번호는 해당 문서에서 재정렬된 이후로는 더 이상 유효하지 않다 — 그 문서의 현재 번호 체계와는 무관한 이력 기록이다.
> 옛 번호 표기 규칙: **「제거·후순위 전환된 항목」 표의 옛 번호와, 참고 이력에서 그 항목들을 열거하는 대목에는 현재 번호와 겹치는지 따지지 않고 예외 없이 `구` 접두어를 붙인다**(2026-09-11 4차 도입). 신규 항목이 뒤에 붙는 순간 "안 겹치는 범위"라는 조건부 규약은 무너지기 때문이다. 예외는 두 가지뿐이다 — ① 「번호 매핑」 표의 「이전 번호」·「1차 번호」 열(열 이름이 이미 옛 번호임을 밝힌다), ② 산문에서 `원 R32` · `1차 F29` 처럼 번호의 세대를 앞에 붙여 쓴 대목.
> 참고 이력 관례: 결정을 기록할 때 **그 결정의 「반영 대상 문서」를 함께 적는다**(예: "반영 대상: 03·04·05·06")(2026-09-11 4차 도입). 한 커밋이 한 문서만 건드리는 팀 구조에서는 이 목록이 전파 누락을 잡는 유일한 체크리스트다.
> 마지막 갱신: 2026-09-11

---

## 01-problem.md

| 후보 | 원 번호 | 출처(누구 초안 · [제안]) | 제외/후순위 사유 |
| ---- | ------ | ------------------------ | --------------- |
| 티어제 랭킹 시스템(브론즈\~다이아몬드로 나누고 티어 안에서 순위) | — | 팀 논의(문서화 전 단계) | 후순위 — MVP는 단순 누적거리 랭킹으로 충분, 이후 확장 후보 |

### 이전 본문 주석

> 2026-09-11에 01-problem.md 본문에 있던 `주:` 주석을 이 문서로 옮겼다. 편집 경위 기록이라 문제 정의 내용 자체는 본문에 그대로 남아 있다.

> 주: 대상을 "개인 러너"에서 "뉴비 러너"로 바꿨다 — 팀 원문 "타겟은 뉴비 러너들이야. 탄천에 사는 뉴비 러너들끼리의 경쟁 시스템인거지."을 그대로 반영한 팀의 직접 지시다. 문제·현재해법·구역판정 룰 등 대상을 가리키지 않는 다른 내용은 바꾸지 않았다.

### 참고 이력

> (2026-09-11 4차) 「MVP 핵심 워크플로우」를 3단계에서 네 단계로 확장해 02의 S0~S8을 모두 담게 했다 — 러닝 3단계(기록 · 구역 판정 · 결과·랭킹) 뒤에 **④ 기록 저장 및 다시 보기(수치 + GPS 이동 경로, 기록 탭 → 기록 상세)** 를 넣고, 가입(S0)·설정(S8)은 러닝 흐름과 구분해 한 줄씩 적었다. 01은 문제 정의 문서이므로 흐름 이름과 한 줄 설명만 두고 구현 세부는 넣지 않았다. 같은 라운드에 ① Zone 판정 시점을 "러닝 종료 시 분석"에서 **"러닝 중 실시간 판정 · 종료 시 확정"** 으로(02 S2·03 R4·04 F2와 맞춤), ② 결과 화면 문구를 "순위 상승 결과"에서 **"현재 순위"** 로(순위 변화 표시는 후순위), ③ 용어를 "탄천 구역"·"탄천 지정 구역"·"탄천 지역 랭킹 거리" → `탄천 Ranking Zone`, "탄천 랭킹 점수" → `탄천 Ranking Zone 인정 누적 거리` 로 통일하고(점수 개념은 어느 문서에도 없다), ④ 다른 6개 문서에만 있던 머리말 2줄을 추가했다. 반영 대상: 01.
> (2026-09-11 5차) 팀이 **회원 관리 기능(회원가입 · 로그인 · 로그아웃 · 회원 탈퇴)을 전부 MVP로 채택**했다. 「진입 · 설정 흐름」의 가입(S0)을 "가입 · 로그인"으로 넓혀 로그인 유지와 재로그인 시 기존 기록 연결을 적고, 설정(S8)에 로그아웃 · 회원 탈퇴(탈퇴하면 계정과 기록 삭제)를 더했다. 반영 대상: 01·02·03·04·05·06.
> (2026-09-11 7차) 팀 결정 — **러닝 중 화면은 현재 페이스, 러닝 종료 후 결과 화면은 평균 페이스**를 보여준다. 01 은 ① 러닝 기록의 실시간 갱신 항목을 "현재 페이스"로 고쳤다. 반영 대상: 01 · 02 · 03 · 04 · 06 · 07 · PRD (이슈 #21).

---

## 02-workflow.md

> **전체 복원** 표기가 있는 행은 후순위가 아니라 **현재 MVP 항목**이며, 되살아난 경위를 추적할 수 있도록 이력 보존용으로만 이 표에 남겨 둔다 — 후순위 판정 기준으로 읽지 말 것(2026-09-11 4차 명시).

| 후보                | 출처(누구 초안 · [제안])           | 제외 이유                              |
| ----------------- | ----------------------- | ---------------------------------- |
| 통산 랭킹             | 02 논의                   | 제외 — 별도 개념으로 채택한 것은 아니며, 주간·월간 구분 없이 누적 거리 기준 단일 랭킹만 제공하기로 한 결정(아래 "주간·월간 랭킹 구분" 행 참고)의 결과로 통산 방식과 유사해진 것일 뿐 |
| 개인 티어·레벨 시스템      | 02 논의                   | 후순위 — 게임화 확장 기능                     |
| 자동 종료             | S3                       | 후순위 — MVP에서는 수동 종료만 지원             |
| 일시정지 · 재개         | S3                       | 제외 — 현재 MVP 러닝 흐름에서는 지원하지 않음       |
| GPS 끊김 구간 보간·추정   | S2 · GPS 정책              | 후순위 — GPS 오류 대응 고도화 영역             |
| 어뷰징 탐지 · 신고       | 02 논의                   | 후순위 — 운영 정책 확장 영역                  |
| 친구 추가 · 팔로우       | 01 MVP 제약                | 후순위 — 핵심 개인 러닝·로컬 랭킹 범위 밖          |
| 러닝 크루 기능          | 01 MVP 제약                | 후순위 — 현재 MVP는 개인 러너 기준             |
| 다음 순위까지 필요한 거리 안내 | S5                       | 후순위 — 추가 동기부여 기능                   |
| 스마트워치 연동          | 02 논의                   | 후순위 — 외부 기기 연동 영역                  |
| 홈 화면 위젯           | 02 논의                   | 후순위 — OS·백그라운드 기능 확장 영역            |
| 고급 GPS 오류 복원      | S2 · GPS 정책              | 후순위 — 핵심 워크플로우 외 기술 고도화            |
| 배터리 최적화           | 02 논의                   | 후순위 — 플랫폼 최적화 영역                   |
| 도발형 동기부여 시스템 고도화  | S3 · R14(구 R32)          | 후순위 — 카피·게임화 정책에서 별도 정의            |
| 음성 페이스 안내 | S2 · 구R10(제거)            | 후순위 — MVP에서는 과한 확장 기능, 추후 개선점으로 재검토. 페이스 자체는 후순위가 아니다 — 러닝 중 실시간 표시(R17·F2)와 결과 화면·기록 상세의 평균 페이스(R14·R21·F9·F11)는 2026-09-10 이후 MVP 항목이며, 여기 남는 것은 음성 안내뿐이다 |
| 러닝 경로 지도 확인       | S6 · 구R26(제거)             | **전체 복원(2026-09-11 2차)** — 1차에서는 러닝 중 지도(R19 · F2)와 결과 화면 경로 지도(R14 · F9)만 되살리고 "기록 탭에서 지난 러닝 경로를 다시 보기"는 후순위로 남겼으나, 2차 결정으로 그 부분까지 MVP에 들어왔다. 이제 러닝 종료 시 GPS 이동 경로가 세션에 함께 저장되고(R8 · F6) 기록 상세(S7 · R21 · F11)에서 다시 조회된다 |
| 지난주·지난달 기록 비교    | S6 · 구R22·구R23·구R24(제거)      | 후순위 — MVP 범위 축소, 이후 개선점으로 재검토 |
| 러닝 완료 후 평균 페이스 표시 | S4 · 구R34(제거)             | **전체 복원(2026-09-10 이후 R14·R17로 MVP 복귀)** — 당시에는 MVP 범위 축소로 빠졌으나, 결과 화면 6항목의 페이스(R14·F9 ⑤)와 러닝 중 실시간 페이스(R17·F2)로 다시 들어와 현재는 MVP 항목이다 |
| 닉네임 자동 랜덤 생성      | S0 · 구R33(현 R15) 일부(제거)   | 후순위 — 닉네임 직접 설정(필수)만 MVP에 남기고, 미입력 시 자동 생성 로직은 제외 |
| 목표 거리·시간 설정       | S0 (원 To-Be)             | 후순위 — MVP 재축소, 목표 설정·달성 확인 없이 러닝 기록만 우선 |
| 주간·월간 랭킹 구분       | S4·S5 (원 To-Be)          | 후순위 — MVP는 누적 거리 기준 단일 랭킹만 제공 |
| 종료 확인 메시지(목표 미달성 시) | S3 (원 To-Be)          | 후순위 — 목표 개념 자체가 빠지면서 함께 후순위 전환 |
| TOP100 캡·100위 밖 별도 표시 | S5 (원 To-Be)          | 후순위 — "누적 거리 순위만 표시, 그 이외 나머지는 모두 후순위" 팀 결정에 따라 랭킹은 전체 순위 리스트만 제공 |

### 정리 이력

> S0의 To-Be에 닉네임 설정 흐름을 추가했다 — 03-requirements.md R15(구 R33)·04-features.md F10(구 F30) 신설에 따른 반영(팀 확정 내용 그대로). 이후 "자동 랜덤 생성" 부분은 후순위 전환되어 "가입 시 직접 설정(필수)"만 남도록 다시 단순화했다.
> S4의 "평균 페이스", S6의 "지난주·지난달 비교"는 후순위 전환되어 To-Be 문구와 mermaid에서 제거했다. (S4의 평균 페이스는 2026-09-10 이후 R14·R17로 MVP에 복귀해 현재 S4 To-Be와 mermaid에 다시 들어가 있다 — 지금도 후순위인 것은 S6의 지난주·지난달 비교뿐이다)
> A2(관리진)는 팀 결정으로 제거되었다 — "A2의 역할이 MVP 시점에 중요하지 않아 빼도 괜찮다." 04-features.md F2(구 F8, 실시간 표시에 병합됨), 05-policy.md 구P8·구SP5가 A2를 참조하고 있어 해당 문서에서도 함께 정리했다.
> 팀의 MVP 재축소 결정으로 목표 거리·시간 설정(S0)·종료 확인 메시지(S3)·주간·월간 랭킹 구분(S4·S5)까지 후순위로 뺐다. 이후 A1 행위자 설명·S5 To-Be에 남아있던 "주간·월간" 언급과, S0·S2·S3의 As-Is/감정/불편 칸에 남아있던 "목표" 프레이밍도 별도로 마저 정리했다(둘 다 사용자가 직접 지적해 잡은 잔여 불일치).
> 03-requirements.md가 R1~R16(이후 R17~R23 추가)으로, 04-features.md가 F1~F13→F1~F10(이후 F11 추가)으로 재정렬되면서 이 문서 각주의 R33·F30·F8 참조를 R15(구 R33)·F10(구 F30)·F2(구 F8)로 갱신했다.
> S1의 GPS 확인 순서를 바꿨다 — 기존엔 "버튼 클릭 → 카운트다운 중 GPS 확인"이었는데, 네트워크가 불안정하면 카운트다운이 끝나고서야 실패를 알게 되는 문제가 있어 "GPS 확인 완료 → 버튼 활성화 → 클릭 → 카운트다운(순수 준비 신호)"으로 바꿨다. 05-policy.md P1은 이 순서 변경과 이미 일치해 따로 고치지 않았다. S2에는 R17(페이스 실시간 확인) 신설에 맞춰 페이스를 추가했다.
> (2026-09-11 1차 — 아래 2차에서 GPS 경로 저장 정책이 뒤집혔다) S2에 지도(현재 위치·Zone 경계·지나온 경로)를, S4에 이번 러닝의 경로 지도를 추가했다. S2의 불편 칸에도 "구역 안에 있는지 숫자만으로는 알기 어렵다"를 함께 넣어 To-Be와 앞뒤가 맞게 했다.
> (2026-09-11 2차) GPS 경로 저장 정책이 뒤집혔다 — 같은 날 1차에서 넣었던 "지도는 S4까지만, 기록에는 수치만 남는다"는 선을 팀이 걷어내고, 러닝 종료 시 경로까지 함께 자동 저장하기로 했다. 이에 맞춰 S1에 시작 전 지도(현재 위치·Zone 위치·Zone 범위)를 추가하고, S4의 "(지도는 이 화면까지만, 기록에는 수치만 남는다)" 문구를 빼고 인정 구간/일반 구간 구분 표시와 자동 저장으로 고쳤다. S6을 누적 거리 2종·최고 기록·세션 목록으로 확장하고, **S7(기록 상세)** 와 **S8(설정 / 프로필 설정 · 보조 흐름, 닉네임 수정)** 을 신설했다. S8은 메인 러닝 흐름 S0→S7에 넣지 않고 mermaid에서도 곁가지로 그렸다.
> (2026-09-11 4차) S0 To-Be의 닉네임 문구에 **"9자 이하 · 한글·영문·숫자만(공백·특수문자 불가)"** 을 넣었다 — S8이 "가입 때와 같은 정책"을 가리키는데 S0에 그 정책이 없어 참조가 공중에 떠 있었다(가입 기능을 새로 정의한 것이 아니라 이미 확정된 R15·P10을 전파한 것이다). S3 To-Be 끝에 "종료 처리 시 세션이 경로까지 포함해 자동 저장된다(F6)"를 넣어 mermaid의 SAVE 노드 위치(S3 직후)와 표를 맞췄고, mermaid 노드 id `R1`·`R2`를 **`RANK_IN`·`RANK_OUT`** 으로 바꿨다 — 요구사항 R1(GPS 게이트)·R2(총 러닝 거리)와 표기가 겹쳐 오독을 불렀다. 반영 대상: 02.
> (2026-09-11 5차) 회원 관리 MVP 편입 — S0을 「회원가입 · 로그인 · 초기 프로필 설정」으로 넓혀 로그인 유지와 재로그인 시 기존 계정 연결(R24·P11)을 To-Be에 넣고, S8을 「설정 / 프로필 · 계정 관리」로 넓혀 로그아웃(R25)·회원 탈퇴(R26·P12·P13)를 넣었다. 두 행 모두 As-Is·감정·불편 칸까지 한 행 전체를 맞췄다. mermaid의 S0·S8 노드 문구를 갱신하고 S8 → S0(로그아웃·탈퇴 후 로그인 화면) 곁가지를 그렸으며, 최종 한 줄에도 반영했다. 반영 대상: 02.
> (2026-09-11 6차 후속) mermaid 아래에 "화면 사이 이동은 07 흐름표를 따른다" 안내 한 줄을 넣었다 — 경위는 07-screens.md 섹션의 「6차 후속」 참고. 반영 대상: 02.
> (2026-09-11 7차) 팀 결정 — **러닝 중 화면은 현재 페이스, 러닝 종료 후 결과 화면은 평균 페이스**를 보여준다. 02 는 S2 To-Be · mermaid S2 를 "현재 페이스"로, S4 To-Be · mermaid S4 를 "평균 페이스"로 고쳤다. 반영 대상: 01 · 02 · 03 · 04 · 06 · 07 · PRD (이슈 #21).

### 이전 본문 주석

> 2026-09-11에 02-workflow.md 본문에 있던 `주:` 주석을 전부 이 문서로 옮겼다. 아래는 옮겨온 원문이며, 내용 자체는 03·04 표 본문에도 반영되어 있다.

> 주: Ranking Zone 좌표는 시스템에 사전 설정되어 있는 것으로 취급하며, 설정 주체는 이 문서 범위 밖이다. *(현재는 04-features.md F2의 입력 칸에 같은 문구가 들어가 있다)*
> 주: 페이스(S4)는 거리·시간처럼 0 처리·GPS 끊김 등의 예외 로직을 적용하지 않고 항상 단순 속력값만 표시한다. *(04-features.md F9 결과 칸에 같은 규칙이 명시되어 있다)*
> 주: 실시간 갱신 원칙 — 러닝 중에는 거리·시간이 실시간으로 갱신되지만, 순위는 실시간으로 갱신되지 않는다(러닝 완료 후 결과 화면·랭킹 화면에서만 확인 가능). S2의 실시간 표시(거리·시간·탄천 인정 거리)와 S4·S5의 순위 확인을 혼동하지 말 것. *(03 R6 본문과 04 F2 결과 칸에 같은 내용이 있다)*

---

## 03-requirements.md

> 2026-09-10에 문서를 재정렬했다. 아래 "번호 매핑"은 살아남은 항목의 이전 번호 ↔ 현재 번호 대응표, "제거·후순위 전환된 항목"은 문서에서 완전히 빠진 항목의 기록이다.
> **제거된 옛 번호에는 현재 번호와 겹치는지 따지지 않고 예외 없이 구R 접두어를 붙인다**(2026-09-11 4차 — 그전까지는 "새 1~16번 범위와 겹치는 것만"이라는 조건부 규약이었는데, 2차에서 R20~R23이 신설되자 구R20·구R22·구R23·구R24가 현행 MVP 요구사항으로 오독되는 문제가 생겼다). 아래 "번호 매핑" 표의 「이전 번호」 열은 열 이름이 이미 옛 번호임을 밝히므로 접두어를 붙이지 않는다. 기존 표기 근거: "구R_" 로 표기해 구분한다(예: 새 R10=개인 최고 기록, 구R10=음성 안내 — 서로 다른 항목).

### 번호 매핑 (생존 항목)

| 이전 번호 | 현재 번호 | 요구사항 요약 |
| -------- | -------- | ------------ |
| R5  | R1  | GPS 사용 불가 시 오류 안내 |
| R6  | R2  | 총 러닝 거리 확인 |
| R7  | R3  | 러닝 시간 확인 |
| R8  | R4  | 탄천 인정 누적 거리 확인 |
| R9  | R5  | GPS 신호 끊김 경고 |
| R11 | R6  | 실시간 갱신·순위 비실시간·GPS 재측정 |
| R19 | R7  | 누적 거리 기준 순위 확인 |
| R21 | R8  | 개인 기록 지속 저장·확인 |
| R25 | R9  | 과거 기록 목록 조회 — **옛 R25다. 현행 R25는 로그아웃(2026-09-11 5차 신설)으로 다른 항목이다** |
| R27 | R10 | 개인 최고 기록 확인 |
| R29 | R11 | Zone 내부 거리만 랭킹 반영 |
| R30 | R12 | Zone 외부 거리도 개인기록 포함 |
| R31 | R13 | 소셜 로그인 |
| R32 | R14 | 결과 화면 6항목(일자 포함) |
| R33 | R15 | 닉네임 직접 설정 |
| R35 | R16 | 3초 카운트다운 |

### 제거·후순위 전환된 항목

> **전체 복원** 표기가 있는 행은 후순위가 아니라 **현재 MVP 항목**이며, 되살아난 경위를 추적할 수 있도록 이력 보존용으로만 이 표에 남겨 둔다 — 후순위 판정 기준으로 읽지 말 것(2026-09-11 4차 명시).

| 구 번호 | 후보 · 내용 | 출처(누구 초안 · [제안]) | 제외/후순위 사유 |
| ------- | ---------- | ------------------------ | --------------- |
| 구R1 | 목표 거리 설정 | 인터뷰 확정(원 R1) | 후순위 — 02의 MVP 재축소로 목표 개념 자체가 빠짐, 목표 설정 없이 러닝 기록만 우선 |
| 구R2 | 목표 시간 설정 | 인터뷰 확정(원 R2) | 후순위 — 구R1과 동일 사유 |
| 구R3 | 목표 값 자동 이어쓰기 | 인터뷰 확정(원 R3) | 후순위 — 구R1·구R2와 함께 목표 개념에 종속되어 제외 |
| 구R4 | 목표 값 변경 | 인터뷰 확정(원 R4) | 후순위 — 구R1·구R2와 함께 목표 개념에 종속되어 제외 |
| 구R10 | 음성 페이스 안내 | 인터뷰 확정(원 R10) | 팀 결정으로 제거 — MVP에서는 구현하지 않고 추후 개선점으로 재검토 |
| 구R12 | 결과 화면 — 총 거리(초안) | (원 R12) | R14(구 R32)로 통합 |
| 구R13 | 결과 화면 — 탄천 인정 거리(초안) | (원 R13) | R14(구 R32)로 통합 |
| 구R14 | 결과 화면 — 시간(초안) | (원 R14) | R14(구 R32)로 통합 |
| 구R15 | 결과 화면 — 개인 순위(초안) | (원 R15) | 02의 단일 랭킹 단순화로 대체, R14(구 R32)에 통합 |
| 구R16 | 결과 화면 — 개인 순위(초안 중복) | (원 R16) | 02의 단일 랭킹 단순화로 대체, R14(구 R32)에 통합 |
| 구R17 | 주간 랭킹 구분 | 인터뷰 확정(원 R17) | 후순위 — 02의 단일 랭킹 단순화로 대체, 대체 항목은 R7(구 R19) |
| 구R18 | 월간 랭킹 구분 | 인터뷰 확정(원 R18) | 후순위 — 위와 동일 사유 |
| 구R20 | TOP100 캡·100위 밖 별도 표시 | 인터뷰 확정(원 R20) | 후순위 — TOP100 캡 개념 자체가 후순위로 빠짐 |
| 구R22 | 지난주 기록 비교 | 인터뷰 확정(원 R22) | 후순위 — MVP 범위 축소, 이후 개선점으로 재검토 |
| 구R23 | 지난달 기록 비교 | 인터뷰 확정(원 R23) | 후순위 — 위와 동일 사유 |
| 구R24 | 총 거리 증감·평균 페이스 변화 비교 | 인터뷰 확정(원 R24) | 후순위 — 구R22·구R23과 함께 MVP 범위 축소 |
| 구R26 | 러닝 경로 지도 확인 | 발산 제안(원 R26, 채택됨) | **전체 복원(2026-09-11 2차)** — 1차에서는 R19(러닝 중 지도)·R14(결과 화면 경로 지도)까지만 되살아났고 "기록 탭에서 지난 러닝 경로 재조회"는 후순위였으나, 2차 결정으로 **R21(과거 러닝 상세)** 로 되살아났다. R8도 GPS 이동 경로를 세션에 함께 저장하도록 개정됐다 |
| 구R28 | 목표 달성 여부 확인 | 인터뷰 확정(원 R28) | 후순위 — 목표 개념 자체가 후순위로 빠지며 함께 제외 |
| 구R34 | 결과 화면 — 페이스(초안) | (원 R34) | R14(구 R32)로 통합 |
| — | 구R33(현 R15)의 자동 랜덤 닉네임 생성 로직 | 인터뷰 확정(원 R33 일부) | 후순위 — 닉네임 직접 설정(필수)만 R15에 남기고, 미입력 시 자동 생성 로직은 제외 |

### 참고 이력

> R7·R8(구 R19·R21)의 유형은 원래 "기능/데이터" 복합값이었으나, 반영 조건(어떤 거리가 랭킹/개인기록에 포함되는지)을 R11·R12(구 R29·R30)로 명시적으로 분리하면서 R7·R8은 "확인할 수 있다"는 화면 기능만 남아 유형을 "기능" 하나로 단순화했다.
> R14(구 R32)는 세 차례 재정의를 거쳐 팀이 최종 확인했다 — ①"러닝 완료 후 축하성 동기부여 메시지" → ②"러닝 종료 시점의 목표 미달성 확인 메시지" → ③(임시 해석) "누적 거리 기준 개인 순위만 확인" → ④(팀 확정) "총 러닝 거리·탄천 인정 거리·개인 순위·러닝 시간·페이스 5항목을 모두 담는 결과 화면 단일 요구사항"(이후 일자가 더해져 현행은 6항목이다). 구R12(총거리)·구R13(탄천 인정 거리)·구R14(시간)·구R15·구R16(주간·월간 순위 변화, 이미 후순위)·구R34(페이스)가 모두 여기 통합되었다.
> R16(3초 카운트다운 확인)은 04-features.md의 F1(당시 F5)에 이미 있던 내용인데 03에 대응 요구가 없어 신규 추가했다(S1 출처).
> (2026-09-10) 문서 전체를 재정렬했다 — 제거·후순위 전환되었던 항목(구R1~구R4·구R10·구R12~구R16·구R17·구R18·구R20·구R22~구R24·구R26·구R28·구R34)을 이 문서로 옮기고, 남은 16개 항목을 R1~R16으로 다시 번호 매겼다.
> (2026-09-10 추가) R17(페이스 실시간 확인)을 신설했다 — 04-features.md의 실시간 표시 기능(F2)에 페이스가 빠져있던 것을 사용자가 지적해 R2·R3·R4와 같은 격으로 추가했다. R2~R4·R17은 모두 같은 화면(S2)에서 함께 실시간 갱신되는 표시 항목이라 04에서는 F2 하나로 묶어 표현한다.
> (2026-09-10 추가) R18(러닝 중 화면의 종료 버튼)을 신설했다 — 원래 R6(실시간 갱신 제약) 문장 안에 "러닝 종료 버튼은 별도로 보여야 한다"가 섞여 있었는데, R6은 제약이고 종료 버튼은 화면 기능이라 R2~R4·R17과 같은 격의 독립 항목으로 떼어냈다. 04에서는 같은 화면이라 F2에 함께 묶었고, 그 버튼을 누르는 동작은 F9(결과 화면)의 트리거로 이어진다.
> (2026-09-10 추가) R1을 "GPS 사용 불가 시 시작하지 않고 오류 안내"에서 "GPS 활성화·확인 완료 상태에서만 시작 버튼이 활성화된다"로 구체화했다 — 04의 F1과 05의 P1이 이미 그 순서로 정의되어 있었는데 03만 조건이 느슨하게 적혀 있어 맞췄다.
> (2026-09-11 1차 — 아래 2차에서 GPS 경로 저장 정책이 뒤집혔다) R19(러닝 중 지도)를 신설했다 — UI 기획 과정에서 경로가 그려지는 지도가 MVP에 더 맞는다고 팀이 판단했다. 현재 위치·탄천 Ranking Zone 경계·지나온 경로 세 가지를 함께 표시하며, 중요도는 `상`. 같은 라운드에 R14(결과 화면)에 "이번 러닝의 경로 지도"를, R8에 "GPS 경로는 개인 기록으로 저장하지 않는다"는 저장 경계를 덧붙였다 — 지도는 러닝 중과 결과 화면까지만 보이고 기록 탭에는 수치만 남는다. 이 선 때문에 구R26(러닝 경로 지도 확인)은 "제거"가 아니라 **부분 복원**으로 정리했다.
> (2026-09-11 2차) 위 1차 결정의 "GPS 경로는 개인 기록으로 저장하지 않는다"는 선을 팀이 폐기했다. R8을 "러닝 종료 시 세션이 자동 저장되고, 저장 대상은 일자·총 러닝 거리·탄천 인정 거리·러닝 시간·평균 페이스·GPS 이동 경로·Ranking Zone 인정 구간 정보"로 개정했고, R14에는 인정 구간/일반 구간 구분 표시를 더했다. 신규로 **R20**(시작 전 지도) · **R21**(과거 러닝 상세) · **R22**(설정 화면에서 닉네임 확인·수정) · **R23**(전체 러닝 총 누적 거리 · 탄천 Ranking Zone 인정 누적 거리)를 추가했다. 기존 R1~R19는 번호를 그대로 두고 재정렬하지 않았다. 이로써 구R26은 **전체 복원**이 됐다.
> (2026-09-11 추가) R15의 닉네임 제한을 팀이 확정했다 — 9자 이하, 특수문자·공백 불가(한글·영문·숫자만). 한도 규칙이라 05-policy.md에 P10으로 신설했고, 03 R15·04 F10 예외·06-data.md 닉네임 칸의 [?]도 함께 해소했다. **반영 대상에 02가 빠져 S0 To-Be 전파가 누락됐다 — 2026-09-11 4차에 잡았다.** 반영 대상: 03·04·05·06(02 누락).
> (2026-09-11 4차) **3차 P9 확대를 03에 전파했다 — 3차가 05·04·06만 고치고 03을 빼먹어 R12와 정면으로 충돌하던 BLOCKER다.** R12 끝에 "비정상 이동 속도 구간(시속 25km 초과)은 Zone 안팎 관계없이 총 러닝 거리·탄천 인정 거리 두 값 모두에서 제외"를, R6에 같은 속도 초과 구간 제외 규칙을, R8에 "속도 초과 구간은 거리 계산에서만 빼고 측정 지점과 경로는 제외 사유와 함께 저장"을 넣었다. R11은 "내부 거리만 반영"이라 P9와 충돌하지 않아 손대지 않았다. 같은 라운드에 R20 끝에 "지도를 확대·축소하고 현재 위치로 되돌릴 수 있어야 한다"를 더하고(04 F1·02 S1에 이미 있던 동작의 요구 근거가 03에 없었다), 머리말의 번호 규칙을 "재정렬하지 않고 신규는 뒤에 붙인다"로 교체하고 채택된 [제안]의 태그를 지운다는 표기 규칙을 한 줄 명시했다. 반영 대상: 03·04 F2·F6 R 열(후속). **같은 라운드 재감사에서 R8 저장 대상과 R6→F 추적에 구멍이 발견돼 이어 고쳤다 — 경위는 06-data.md 섹션의 「4차 후속」 항목 참고.**
> (2026-09-11 5차) 회원 관리 MVP 편입 — **R24**(로그인 유지 · 재로그인 시 기존 계정 연결, S0) · **R25**(설정 화면에서 로그아웃, S8) · **R26**(확인 절차를 거친 회원 탈퇴와 즉시 삭제, S8)를 뒤에 붙였다. R13("가입·로그인")은 그대로 두고 R24가 그 "로그인"을 구체화한다. 번호 겹침: 현행 R24·R26은 제거표의 구R24(총 거리 증감 비교)·구R26(러닝 경로 지도)과, 현행 R25는 매핑표의 이전 번호 R25(현 R9)와 숫자가 같지만 서로 다른 항목이다. 이로써 지난 감사에서 보류한 F-05(재로그인 흐름 부재)가 닫힌다. 반영 대상: 03·04 F8·F12·F13.
> (2026-09-11 7차) 팀 결정 — **러닝 중 화면은 현재 페이스, 러닝 종료 후 결과 화면은 평균 페이스**를 보여준다. 재감사 「확인 필요」(R17 "현재 페이스" vs 06 · 07 "평균 페이스")를 닫는다. 03 은 R6 의 실시간 갱신 항목을 "현재 페이스"로, R14 결과 화면을 "평균 페이스(속력)"로 고치고, R17 에 "최근 이동 구간 기준 · 결과 화면은 평균 페이스(R14)"를 덧붙였다. 반영 대상: 01 · 02 · 03 · 04 · 06 · 07 · PRD (이슈 #21).

### 이전 본문 주석

> 2026-09-11에 03-requirements.md 본문에 있던 `주:` 주석을 전부 이 문서로 옮겼다. 둘 다 편집 경위·열 값 설명이라 스펙 내용은 표 본문에 그대로 남아 있다.

> 주: R6은 "순위는 러닝 중에는 보여줄 필요가 없다"는 내용을 각주가 아닌 본문에 직접 명시했다(팀 요청).
> 주: R11·R12는 반영 조건(어떤 거리가 랭킹/개인기록에 포함되는지)을 명시적으로 분리한 제약 요구사항이라, R7·R8은 "확인할 수 있다"는 화면 기능만 남아 유형을 "기능" 하나로 단순화했다.

---

## 04-features.md

> 04는 2026-09-10에 두 차례 재정렬을 거쳤다 — 1차는 제거·후순위 항목을 빼는 정리(원 번호 → F1~F13), 2차는 성격이 같은 기능들을 하나로 합치는 정리(F1~F13 → F1~F10, 이후 F11 추가).
> **제거된 옛 번호에는 현재 번호와 겹치는지 따지지 않고 예외 없이 구F 접두어를 붙인다**(2026-09-11 4차 — 03 섹션과 같은 규칙이다. 신규 F가 뒤에 붙는 순간 "안 겹치는 범위"라는 조건부 규약은 무너진다). 아래 "번호 매핑" 표의 「이전 번호」·「1차 번호」 열은 열 이름이 이미 옛 번호임을 밝히므로 접두어를 붙이지 않는다. 기존 표기 근거: 제거된 구번호 중 구F1~구F4·구F10·구F12·구F13은 1차 기준 새 번호 범위와 숫자가 겹쳐 "구F\_" 로 표기해 구분한다.

### 번호 매핑 — 1차 재정렬(원 번호 → F1~F13)

| 이전 번호 | 1차 번호 | 기능 요약 |
| -------- | -------- | -------- |
| F5  | F1  | GPS 확인 후 3초 카운트다운·러닝 시작 |
| F6  | F2  | 총 러닝 거리 실시간 표시 |
| F7  | F3  | 러닝 시간 실시간 표시 |
| F8  | F4  | 탄천 인정 누적 거리 실시간 표시 |
| F9  | F5  | GPS 신호 끊김 경고 표시 |
| F11 | F6  | GPS 신호 끊김→복구 처리 |
| F19 | F7  | 랭킹 화면(전체 순위 리스트) |
| F21 | F8  | 개인 기록 저장·조회 |
| F24 | F9  | 과거 기록 목록("기록" 탭) |
| F26 | F10 | 개인 최고 기록(PR) 표시 |
| F28 | F11 | 소셜 로그인 |
| F29 | F12 | 결과 화면 6항목(일자 포함) — **1차 F12다. 현행 F12는 로그아웃(5차 신설)으로 다른 항목이다** |
| F30 | F13 | 닉네임 입력 — **1차 F13이다. 현행 F13은 회원 탈퇴(5차 신설)로 다른 항목이다** |

### 번호 매핑 — 2차 재정렬(F1~F13 → F1~F10, 이후 F11 추가)

| 1차 번호 | 현재 번호 | 내용 |
| -------- | -------- | ---- |
| F1 | F1 | (번호 유지, 내용만 개정) GPS 확인 완료 후 버튼 활성화 → 클릭 → 카운트다운(준비 신호)으로 순서 변경 |
| F2, F3, F4 | F2 | 병합 — "러닝 중 실시간 표시"로 통합(총거리·탄천인정거리·시간 + 신규 페이스) |
| F5 | F3 | GPS 신호 끊김 경고 |
| F6 | F4 | GPS 신호 끊김→복구 |
| F7 | F5 | 랭킹 화면 |
| F8 | F6 | 개인 기록 저장·조회("기록 탭" 용어로 통일) |
| F9, F10 | F7 | 병합 — "기록 탭"으로 통합(과거 목록 + 동적 개인 최고 기록) |
| F11 | F8 | 소셜 로그인 |
| F12 | F9 | 결과 화면 6항목(일자 포함) — **1차 F12다. 현행 F12는 로그아웃으로 다른 항목이다** |
| F13 | F10 | 닉네임 입력 — **1차 F13이다. 현행 F13은 회원 탈퇴로 다른 항목이다** |

### 제거·후순위 전환된 항목

> **전체 복원** 표기가 있는 행은 후순위가 아니라 **현재 MVP 항목**이며, 되살아난 경위를 추적할 수 있도록 이력 보존용으로만 이 표에 남겨 둔다 — 후순위 판정 기준으로 읽지 말 것(2026-09-11 4차 명시).

| 구 번호 | 후보 · 내용 | 제외/후순위 사유 |
| ------- | ---------- | --------------- |
| 구F1 | 목표 거리 설정 UI | 후순위 — 02의 MVP 재축소로 목표 개념 자체가 빠짐, 목표 설정 없이 러닝 기록만 우선 |
| 구F2 | 목표 시간 설정 UI | 후순위 — 구F1과 동일 사유 |
| 구F3 | 목표 값 자동 불러오기 | 후순위 — 구F1·구F2와 함께 목표 개념에 종속되어 제외 |
| 구F4 | 목표 값 변경(러닝 시작 화면 톱니바퀴 팝업) | 후순위 — 구F1·구F2와 함께 목표 개념에 종속되어 제외 |
| 구F10 | 음성 페이스 안내 | 팀 결정으로 제거 — MVP에서는 구현하지 않고 추후 개선점으로 재검토 |
| 구F12 | 결과 화면 — 총 거리(초안) | 현 F9로 통합됨 |
| 구F13 | 결과 화면 — 탄천 인정 거리(초안) | 현 F9로 통합됨 |
| 구F14 | 결과 화면 — 시간(초안) | 현 F9로 통합됨 |
| 구F15 | 주간 순위 변화 표시 | 후순위 — 02의 단일 랭킹 단순화로 대체, 대체 항목은 현 F9 |
| 구F16 | 월간 순위 변화 표시 | 후순위 — 위와 동일 사유 |
| 구F17 | 주간 랭킹 화면 | 후순위 — 02의 단일 랭킹 단순화로 대체, 대체 항목은 현 F5 |
| 구F18 | 월간 랭킹 화면 | 후순위 — 위와 동일 사유 |
| 구F20 | TOP100 밖 별도 표시 | 후순위 — TOP100 캡 개념 자체가 후순위로 빠짐 |
| 구F22 | 지난주·이번 주 기록 비교 | 후순위 — MVP 범위 축소, 이후 개선점으로 재검토 |
| 구F23 | 지난달·이번 달 기록 비교 | 후순위 — 위와 동일 사유 |
| 구F25 | 러닝 경로 지도 확인 | **전체 복원(2026-09-11 2차)** — 1차에서는 F2(러닝 중)·F9(결과 화면)에만 흡수됐고 기록 탭의 과거 경로 지도는 후순위였으나, 2차 결정으로 **F11(기록 상세)** 신설과 함께 전부 MVP로 들어왔다. F6은 이제 GPS 이동 경로와 Ranking Zone 인정 구간 정보까지 저장한다. 기존에 함께 묶여 있던 "GPS 끊김 구간 특수 표시" 세부 항목은 **후순위 유지** — 끊긴 구간은 표시하지 않고 임의 연결·보간도 하지 않는 것이 현 정책이다(P2) |
| 구F27 | 목표 달성 여부 표시 | 후순위 — 목표 개념 자체가 후순위로 빠지며 함께 제외 |
| 구F31 | 결과 화면 — 페이스(초안) | 현 F9로 통합됨 |
| — | 개인 기록 저장 실패 시 별도 안내(1차 F21, 현 F6 관련) | 후순위 — 기술적 에러 처리 영역, 이 기능 문서 범위 밖 |
| — | 과거 기록 목록 정렬·필터 옵션(1차 F24, 현 F7 관련) | 후순위 — 조회 편의 확장 기능, MVP는 기본 목록 조회만 |
| — | 1차 F30(현 F10)의 자동 랜덤 닉네임 생성 로직 | 후순위 — 닉네임 직접 입력(필수)만 MVP에 남기고, 미입력 시 자동 생성 로직은 제외 |

### 참고 이력

> 원 F24~F27(과거 기록 목록·지도·PR·목표 달성)은 2026년 어느 라운드에 구R24 통합 정리 과정에서 번호가 한 칸씩 당겨진 적이 있다 — "번호는 절대 재정렬하지 않는다" 규칙이 공식화되기 전(05-policy.md 작성 전)의 예외였고, 당시엔 되돌리지 않았다. 이번 2026-09-10 전면 재정렬로 그 예외 이력 자체도 의미가 없어졌다.
> 현 F9(결과 화면, 1차 F29)는 세 차례 재정의를 거쳐 팀이 최종 확인했다 — ①"목표 미달성 종료 확인 메시지" → ②(임시 해석) "결과 화면에 개인 순위만 표시" → ③(팀 확정) "총 거리·탄천 인정 거리·개인 순위·러닝 시간·페이스 5항목을 모두 표시하는 결과 화면 단일 기능"(이후 일자가 더해져 현행은 6항목이다). 구F12(총 거리)·구F13(탄천 인정 거리)·구F14(시간)·구F15·구F16(주간·월간 순위 변화, 이미 후순위)·구F31(페이스)이 모두 여기 통합되었다.
> 과거 05-policy.md의 구P5 대상 F는 "구F12,구F13,구F14,1차 F29,구F31" → "1차 F29" 하나로 단순화되었고, 1차 P11 대상 F도 "1차 F8,구F13" → "1차 F8,1차 F29"로 갱신된 적이 있다(탄천 인정 거리 표시가 F29로 옮겨갔기 때문). 지금 기준으로는 각각 P4, P9가 대상 F를 F2, F9로 갖는 것에 해당한다.
> (2026-09-10, 2차 재정렬) F1은 팀이 GPS 확인 순서를 바꾸기로 하면서 번호는 그대로 두고 트리거·결과·예외 내용만 다시 썼다 — 기존엔 "버튼 클릭 → 카운트다운 중 GPS 확인"이라 네트워크가 불안정하면 카운트다운이 끝나고서야 GPS 실패를 알게 되는 문제가 있었는데, "GPS 확인 완료 → 버튼 활성화 → 클릭 → 카운트다운(순수 준비 신호)"으로 순서를 바꿨다. F2(옛 F2·F3·F4 병합)는 페이스 실시간 표시(R17 신설)를 포함하도록 확장됐고, F7(옛 F9·F10 병합)은 "기록 탭"이라는 화면명으로 명확히 통일됐다 — 옛 F10의 트리거가 "러닝 완료 화면 진입"으로 되어 있어 방금 러닝을 마친 직후의 화면(F9)과 혼동될 여지가 있었기 때문이다.
> F6·F7·F9는 서로 다른 화면(러닝 종료 처리 / 기록 탭 / 결과 화면)이지만 "종료 버튼 → 결과 화면 → 자동 저장 → 랭킹 반영"이라는 하나의 흐름으로 이어진다는 점을 각 항목 설명에 명시적으로 교차 참조해뒀다(행을 합치지는 않기로 함).
> F5는 "탄천 Ranking Zone 누적 거리 기준 전체 순위 리스트"로 단순화되며 TOP100 캡 개념이 빠졌다. F10(닉네임)은 "자동 랜덤 생성" 로직만 빠지고 "직접 입력(필수)"만 남았다.
> (2026-09-11 1차 — 아래 2차에서 GPS 경로 저장 정책이 뒤집혔다) 지도는 별도 F를 만들지 않고 **화면 단위로 나눠 넣었다** — 러닝 중 지도는 F2(같은 화면의 실시간 표시 항목이라 병합), 결과 화면 경로 지도는 F9. F6에는 "수치만 저장하고 GPS 경로는 저장하지 않는다"를, F2 예외에는 "GPS 끊김 시 위치 핀이 멈추고 경로 선이 끊긴다"(F4와 동일 규칙)를 명시했다. **F7(기록 탭)에는 지도를 넣지 않았다** — 과거 경로 재조회는 여전히 후순위다.
> (2026-09-11 2차) 위 1차 결정이 뒤집혀 F6이 GPS 이동 경로와 Ranking Zone 인정 구간 정보까지 저장하게 됐고, **F11(기록 상세)** 을 신설해 과거 세션의 경로를 다시 볼 수 있게 했다. F1에는 시작 전 지도(R20), F7에는 누적 거리 2종(R23)과 F11로 이동하는 동선, F9에는 인정 구간/일반 구간 구분 표시를 넣고 "지도는 이 화면까지만 / 경로는 저장되지 않는다" 문구를 지웠다. 저장 시점도 "화면을 벗어나면"에서 **"러닝 종료 처리 시"** 로 통일했다. F10은 최초 설정과 가입 후 닉네임 수정(R22) 두 트리거를 갖도록 확장했다. 기존 F1~F10은 번호를 그대로 두고 재정렬하지 않았다.
> (2026-09-11 4차) R→F 추적이 끊겨 있던 두 제약을 R 열에 올렸다 — **F6에 R12**(Zone 외부 거리도 개인 기록에 포함), **F9에 R11**(Zone 내부 거리만 랭킹 반영). 둘 다 결과 칸 산문에만 있어 F 단위로 티켓을 끊으면 추적되지 않았다. F9 입력 칸의 "탄천 구역 내 누적 거리"를 `탄천 Ranking Zone 인정 거리` 로 통일하고, 머리말의 번호 규칙 교체와 [제안] 태그 규칙을 03과 같은 문장으로 넣었다. 「정책(P)」·「우선순위 2축」·「인수 조건」 공란은 팀 결정 대기라 그대로 뒀다. 반영 대상: 04. **같은 라운드 재감사에서 F2·F6 의 R 열 R6 누락과 F6↔F9 의 순위 계산 주체 모순이 발견돼 이어 고쳤다 — 경위는 06-data.md 섹션의 「4차 후속」 항목 참고.**
> (2026-09-11 5차) 회원 관리 MVP 편입 — **F8** 트리거를 ① 최초 가입 ② 재로그인으로 넓히고 R 열에 R24를 더했으며, 예외에 "가입 중 이탈 시 닉네임 설정부터 재개"를 넣었다(P11). 신규 **F12**(로그아웃 · R25)와 **F13**(회원 탈퇴 · R26)를 뒤에 붙였다. 번호 겹침: 현행 F12·F13은 매핑표의 1차 번호 F12(현 F9)·F13(현 F10), 제거표의 구F12·구F13(결과 화면 초안)과 숫자가 같지만 서로 다른 항목이다. 반영 대상: 04·05 P11~P13·06.
> (2026-09-11 6차 후속) F2 예외(앱 재실행 시 진행 중 세션 복귀) · F7 · F9 결과(본인 닉네임) · F8 트리거 ①(로그인 화면 진입) · F8 예외(다음 앱 실행 또는 로그인 때) · F13 결과(삭제 범위를 06 에 위임) — 경위는 07-screens.md 섹션의 「6차 후속」 참고. 반영 대상: 04.
> (2026-09-11 7차) 팀 결정 — **러닝 중 화면은 현재 페이스, 러닝 종료 후 결과 화면은 평균 페이스**를 보여준다. 04 는 F2 의 4항목을 "현재 페이스(최근 이동 구간 기준 · 저장하지 않는 계산값)"로, F9 ⑤ 를 "평균 페이스"로 고쳤다. 반영 대상: 01 · 02 · 03 · 04 · 06 · 07 · PRD (이슈 #21).

---

## 05-policy.md

> 2026-09-10에 문서를 재정렬했다.
> **제거된 옛 번호에는 현재 번호와 겹치는지 따지지 않고 예외 없이 구P / 구SP 접두어를 붙인다**(2026-09-11 4차 — 03·04 섹션과 같은 규칙이다). 아래 "번호 매핑" 표의 「이전 번호」 열은 열 이름이 이미 옛 번호임을 밝히므로 접두어를 붙이지 않는다 — 다만 그 열의 값이 현행 번호와 겹칠 수 있으니 조회할 때 주의한다. 기존 표기 근거: 제거된 구번호 중 구P1·구P8·구SP1은 새 번호 범위와 숫자가 겹치므로 "구P\_"/"구SP\_"로 표기해 구분한다(예: 새 P8=동시 종료 처리 순차화, 구P8=A2 좌표 설정 권한 — 서로 다른 정책).

### 번호 매핑 (생존 항목)

| 이전 번호 | 현재 번호 | 내용 |
| -------- | -------- | ---- |
| P2  | P1 | GPS 정상이어야 러닝 시작 가능 |
| P3  | P2 | GPS 끊긴 구간은 거리 계산 제외 |
| P4  | P3 | GPS 신호 끊김 2초 이상 시 경고 |
| P5  | P4 | 종료 전에는 결과 화면 접근 불가 |
| P6  | P5 | 랭킹 산정 규칙(Zone 내부 기준·동일 거리 우선순위·프로필 비공개) |
| P7  | P6 | 개인 기록 조회 권한(무제한 저장) |
| P9  | P7 | 동시 세션 1개 제한 — **옛 P9다. 현행 P9는 비정상 이동 속도 구간 제외로 다른 항목이다** |
| P10 | P8 | 동시 종료 처리 순차화(타임스탬프 기준) — **옛 P10이다. 현행 P10은 닉네임 정책으로 다른 항목이다** |
| P11 | P9 | 비정상 이동 속도 구간 제외 — **옛 P11이다. 현행 P11은 소셜 로그인 · 계정 연결 정책(2026-09-11 5차 신설)으로 다른 항목이다** |
| SP2 | SP1 | 다른 사용자 프로필 비공개 |
| SP3 | SP2 | 크루·팀 기능 없음 — **옛 SP3이다. 현행 SP3은 개인정보 수집·공개 범위(2026-09-11 4차 신설)로 다른 항목이다** |

### 제거·무효화·통합된 항목

| 구 번호 | 내용 | 사유 |
| ------- | ---- | ---- |
| 구P1 | 목표 거리·시간 범위 제한(1km~100km, 5분~360분) | 무효화 — 04의 구F1·구F2·구F4(목표 거리·시간 설정)가 후순위 전환되어 전제가 사라짐. 목표 개념이 부활하면 이 규칙을 되살릴 수 있다 |
| 구P8 | Ranking Zone 좌표는 A2(관리진)만 설정 가능 | 무효화 — 02-workflow.md에서 A2가 팀 결정으로 제거되어 전제가 사라짐. Ranking Zone 좌표 설정 주체는 문서 범위 밖으로 둠 |
| 구SP1 | 랭킹 초기화와 개인 기록 보존은 별개(주간·월간 랭킹 초기화되어도 개인 기록은 유지) | 무효화 — 주간·월간 랭킹 구분 자체가 후순위로 빠지며 "주기적 초기화" 개념이 사라짐 |
| 구SP4 | 동시 세션 제한(자체 제안) | 구P9(현 P7)와 내용이 완전히 중복되어 별도 유지하지 않고 통합됨 |
| — | TOP 100 표시 규칙(원 구P6 일부) | 후순위 — TOP100 캡 개념 자체가 후순위로 빠짐 |
| — | 주간·월간 랭킹 집계 상태값(초기화 시각 포함) | 후순위 — 주간·월간 구분 자체가 후순위로 빠짐, 구분 부활 시 재사용 |

### 참고 이력

> 구P8·구SP5(2026-09-10 재정렬 전의 번호)는 02-workflow.md의 A2(관리진) 제거로 무효화되어 한동안 번호만 유지한 채 내용을 비워뒀었다. 같은 방식으로 구P1도 목표 개념 제거로, 구SP1도 주간·월간 랭킹 구분 제거로 무효화되어 있었다 — 이번 재정렬에서 네 항목 모두 본문에서 완전히 빼고 이 표로 옮겼다.
> 04-features.md 섹션의 "구P5,구P11 대상 F 갱신 이력"은 옛 번호 기준 서술이다 — 현재 번호로는 P4(결과 화면 접근 제한)·P9(비정상 속도 제외)에 해당한다.
> (2026-09-10) 문서 전체를 재정렬했다 — 무효화·통합·후순위 전환되었던 항목(구P1·구P8·구SP1·구SP4)을 이 문서로 옮기고, 남은 9개 정책을 P1~P9로, 남은 2개 서비스 정책을 SP1~SP2로 다시 번호 매겼다.
> (2026-09-10 추가) 04-features.md가 F2·F3·F4를 F2로, F9·F10을 F7로 합치는 2차 재정렬을 거치면서, 05의 대상 F열도 다시 갱신했다(좌변은 1차 번호다: F2,F4,F6→F2,F4 / F5→F3 / 1차 F12→F9 / F7→F5 / F8,F9,F10→F6,F7 / F7,1차 F12→F5,F9 / F4,1차 F12→F2,F9).
> (2026-09-11 2차) GPS 경로 영구 저장과 F11(기록 상세) 신설에 맞춰 기존 정책의 대상 F와 문구만 넓혔고, 번호는 새로 매기지 않았다 — P2 대상 F에 F6·F9·F11을 더하고 "저장 경로에서도 제외 · 과거 상세에서도 임의 연결·보간 금지"를 넣었다. P6 대상 F에 F11을 더하고 조회 범위를 누적 기록·과거 세션·세션 상세·저장된 경로로 명시했다. P10은 적용 시점을 "최초 설정"과 "가입 후 설정 화면에서 닉네임 수정" 두 경우로 넓히고 수정 실패 시 기존 닉네임 유지를 못박았다. P5의 닉네임 참조와 SP1의 지도 대상 화면(F1·F2·F9·F11)도 함께 맞췄다. 새 P는 만들지 않았다.
> (2026-09-11 3차) P9(비정상 이동 속도)의 적용 범위를 팀이 확정했다 — 기존에는 "Ranking Zone 내부 이동 중"에 "탄천 인정 거리 계산에서만" 제외였는데, **Zone 안팎을 가리지 않고 총 러닝 거리와 탄천 인정 거리 두 값 모두에서 제외**하는 것으로 넓혔다. 다만 해당 GPS 측정 지점은 실제로 측정된 것이라 제외 사유와 함께 저장하고 지도에도 그대로 그린다 — 지점 자체가 없는 P2(GPS 끊김)와 다르게 다룬다. 대상 F에 F6을 더했고, 04 F2·F6 예외 칸과 06의 「총 러닝 거리」·「탄천 인정 거리」·「거리 계산 제외 여부·사유」 칸에 함께 반영했다. **반영 대상에서 03이 빠져 R12와 충돌하는 BLOCKER가 됐다 — 2026-09-11 4차에 잡았다.** 반영 대상: 04·05·06(03 누락).
> (2026-09-11 4차 — 아래 4차 후속에서 수집·공개 열거가 다시 열렸다) **SP3(개인정보 수집·공개 범위)** 를 신설했다 — GPS 이동 경로를 영구 저장하기로 한 2차 결정 이후에도 수집 범위가 어느 문서에도 없던 것을 팀이 정책으로 못박았다. 수집은 **소셜 계정 식별자 · 닉네임 · 러닝 기록(수치와 GPS 이동 경로)** 으로 한정하고, 그 밖에 서비스가 스스로 발급·기록하는 값은 사용자 UID와 가입 일시뿐이며(둘 다 사용자 화면에 표시하지 않는다), 본인 외에 공개하는 값은 **닉네임과 랭킹 누적 거리**뿐이다. 머리말의 번호 규칙 교체와 [제안] 태그 규칙도 03·04와 같은 문장으로 넣었다. P1~P10은 손대지 않았다(P9는 3차에서 이미 확정된 상태다). 반영 대상: 05·06(가입 일시 판정과 맞물림). **같은 라운드 재감사에서 SP3 의 닫힌 열거가 06 저장 항목과 상충하는 것이 발견돼 이어 고쳤다 — 경위는 06-data.md 섹션의 「4차 후속」 항목 참고.**
> (2026-09-11 5차) 회원 관리 MVP 편입 — **P11**(F8: 인증 전 계정 생성·로그인 불가 · 소셜 계정 식별자 1개 ↔ UID 1개 · 재로그인 시 기존 UID · 가입 중이면 닉네임 설정부터), **P12**(F12·F13: 진행 중 러닝 세션이 있으면 로그아웃·탈퇴 불가), **P13**(F13: 확인 절차 · 즉시 삭제 범위 · 재가입 시 새 UID · 닉네임 해제)를 뒤에 붙였다. 「상태값」에 **계정**(미가입 → 가입 중 → 가입 완료 → 탈퇴)과 **로그인**(로그아웃 ↔ 로그인) 상태를 더하고, 정의되지 않은 상태값을 쓰던 P6("상시" → 계정 상태 = 가입 완료)·P8(→ 러닝 세션 상태가 "종료"로 전이될 때)·P10(①② → 계정 상태 가입 중 / 가입 완료)을 고쳤다. SP3에 "탈퇴하면 수집·운영 값을 즉시 삭제한다 · P13"을 넣었다. 번호 겹침: 현행 P11은 매핑표의 이전 번호 P11(현 P9)과 숫자가 같지만 서로 다른 항목이다. 이로써 지난 감사에서 보류한 F-04(F8 대응 정책 없음)·F-12(계정 상태값 부재)가 닫힌다. 반영 대상: 05·04·06.
> (2026-09-11 6차 후속) P2 「언제」 · P11 대상 F(F8, F10) · P13 삭제 범위 위임 · 러닝 세션 상태값(앱 재실행 시 진행 중 세션 유지) · 로그인 전이(탈퇴) — 경위는 07-screens.md 섹션의 「6차 후속」 참고. 반영 대상: 05.

---

## 06-data.md

> 06-data.md는 「검토했으나 제외」 표를 문서 안에 그대로 둔다 — 다른 문서와 달리 저장 항목 후보는 같은 표의 다른 행과 나란히 놓고 봐야 제외 근거가 읽히기 때문이다. 따라서 이 섹션에는 후보 표를 두지 않고 참고 이력만 남긴다.

### 참고 이력

> (2026-09-11 3차) 사용자 식별 방식을 팀이 확정했다 — **고유 사용자 UID**를 발급해 러닝 세션·개인 최고 기록·랭킹 집계가 그 값으로 사용자를 가리킨다. R22로 닉네임이 수정 가능해지면서 닉네임으로 식별하면 수정 순간 과거 기록과 랭킹 누적이 끊기기 때문이다. 함께 검토 중이던 「소셜 계정 식별자」 [제안]도 채택해, 재로그인 때 그 값으로 UID를 찾는 역할로 확정했다. 03에는 새 R을 만들지 않았다 — 사용자에게 보이는 요구가 아니라 저장 구조 결정이라 06-data.md에만 적었다. *(이 기록은 원래 03-requirements.md 섹션에 들어가 있었다 — 06의 결정이므로 2026-09-11 4차에 이 섹션으로 옮겼다.)* 반영 대상: 06.
> (2026-09-11 4차) 판정 대기 상태였던 `[제안]` 5건을 팀이 전부 판정해 이 문서의 `[제안]` 표기를 0건으로 만들었다. **채택 4건** — ① 사용자·가입 일시: 저장하되 **사용자 화면에는 표시하지 않는 운영·개발용 값**으로 확정하고 쓰는 기능을 `F8` 만 남겼다(F10 제거), ② 러닝 세션·결과 화면에 표시한 개인 순위: 종료 처리 시점의 계산값을 세션에 함께 저장한다, ③ 러닝 세션·개인 최고 기록 갱신 여부: F9가 결과 화면에서 PR 안내를 띄울지 판정하는 값으로 확정, ④ 비기능 절의 GPS 신호 상태 미저장 판단: 화면 경고용 일시 상태이며 저장 항목이 아니라고 확정(저장이 필요한 구간 정보는 「거리 계산 제외 여부 · 사유」 칸이 대신한다). **기각 1건** — 개인 최고 기록의 「각 기록을 세운 일자」: MVP에서는 최고 기록을 언제 세웠는지 표시하지 않기로 해 행을 지우고 이 문서의 「검토했으나 제외」 표로 옮겼다. 같은 라운드에 「Ranking Zone 설정 · 구역 경계 좌표」 칸에서 `P9` 를 지우고(3차 이후 P9는 Zone 판정과 무관하고 속도 계산은 「기록 시각」 기준이다) 쓰는 기능을 `F1, F2` 로 줄였다 — F5는 좌표를 쓰지 않고, F9·F11의 인정 구간/일반 구간 구분은 지점별 「Ranking Zone 내부 여부」를 쓴다. 반영 대상: 06·05(SP3 신설과 맞물림)·03·04(아래 후속 참고).
> (2026-09-11 4차 후속 — 같은 라운드 재감사에서 잡은 것) 위 4차 반영에 세 군데 구멍이 있어 같은 라운드에 이어 닫았다. ① 채택한 두 세션 칸(결과 화면에 표시한 개인 순위 · 개인 최고 기록 갱신 여부)이 03 R8 저장 대상 열거와 04 F6 입력·결과에 없어, 저장 주체 없이 쓰는 기능만 있는 칸이 돼 있었다 — R8·F6·F9에 전파하고 06의 쓰는 기능에 `F6`을 더했다. ② 신설한 SP3의 수집 범위가 닫힌 열거문이어서 06의 「소셜 로그인 제공자」·세션 파생값과 상충했다 — 열거를 열고 「대조: 06-data.md 「저장 항목」 전수」를 달았으며, 공개 값에 순위를, 용어를 `탄천 Ranking Zone 인정 누적 거리` 로 맞췄다. ③ 옛 번호 `구` 접두어 무조건 규약을 03·04 섹션에만 넣어 05 섹션이 옛 조건부 규약으로 남아, 그 사이 신설된 현행 SP3·P10이 매핑표의 옛 SP3·P10과 조용히 충돌했다 — 05 섹션 규약을 교체하고 세 행(옛 P9·P10·SP3)에 "현행은 다른 항목" 주석을 달았다. 이어진 2차 재감사에서 네 건을 더 닫았다 — ④ R8·F6 의 저장 대상 열거가 "~다" 로 닫혀 있어 종료 타임스탬프·세션 상태가 빠져 보였다(전체 칸은 06 을 단일 기준으로 위임했다), ⑤ 순위 계산 주체가 F6(06 기준)과 F9(04 기준)로 갈려 순환 의존이 됐다(**F6 이 계산·저장하고 F9 는 표시만 한다**로 고정하고 F5 서술도 맞췄다), ⑥ R6 에 새로 넣은 속도 초과 조항이 F2·F6 의 R 열에 없어 R11·R12 와 같은 유형의 추적 끊김이 남아 있었다, ⑦ 개인 최고 기록 3항목과 「갱신 여부」의 쓰는 기능이 04 서술과 어긋나 있었다(갱신은 F6·표시는 F7·판정값만 F9 로 정리). 02 mermaid 의 결정 노드 id `R` 도 `ZONE_CHK` 로 바꿨다. 반영 대상: 00·02·03·04·05·06.
> (2026-09-11 5차) 회원 관리 MVP 편입 — 「사용자 · 계정 상태」 칸을 새로 두고(가입 중 → 가입 완료 · 탈퇴는 저장값이 아니라 즉시 삭제), 「사용자 UID」의 쓰는 기능에 F12·F13과 "탈퇴 시 삭제 · 재가입 시 새 UID"를, 「소셜 계정 식별자」에 R24·P11(유일 연결)·P13과 F13을 더했다. 비기능 절에 **탈퇴 시 삭제 범위**와 **로그인 유지는 기기 쪽 상태라 저장 항목이 아니다**는 판단을 한 줄씩 넣었다. 머리말 범위를 F1~F13으로 고쳤다. 반영 대상: 06·05 SP3.
> (2026-09-11 6차 후속) 세션 상태(F12 · F13 · P12 · 재실행 시 유지) · 계정 상태(F6 · F7 · F11 · P6) · 소셜 로그인 제공자 · 닉네임 · 가입 일시(F13 · P13) 칸과 머리말 신설 항목 수 — 경위는 07-screens.md 섹션의 「6차 후속」 참고. 반영 대상: 06.
> (2026-09-11 7차) 팀 결정 — **러닝 중 화면은 현재 페이스, 러닝 종료 후 결과 화면은 평균 페이스**를 보여준다. 06 은 「평균 페이스(속력)」 칸의 쓰는 기능에서 F2 를 빼고 "결과 화면 · 기록에 보이는 값"임을 적었다. 비기능 절에 "현재 페이스는 최근 GPS 측정 지점으로 계산해 화면에만 보여 주는 값이며 저장하지 않는다"를 넣고, 근거가 없는 계산 구간(최근 몇 초 · 몇 m)은 [?] 로 남겼다(머리말 [?] 2 → 3). 반영 대상: 01 · 02 · 03 · 04 · 06 · 07 · PRD (이슈 #21).

---

## 07-screens.md

> 07-screens.md 는 06-data.md 와 마찬가지로 「검토했으나 제외」 표를 문서 안에 둔다. 이 섹션에는 참고 이력만 남긴다.

### 참고 이력

> (2026-09-11 6차) planning-partner 일괄 작성으로 신설했다 — MVP F1~F13 을 화면 9개(로그인 · 닉네임 설정 · 러닝 시작 · 러닝 진행 · 결과 · 랭킹 · 기록 탭 · 기록 상세 · 설정)로 묶고, 02 mermaid 와 04 트리거·결과·예외의 이동을 흐름표로 옮겼다. 발산 [제안] 8건은 팀 위임에 따라 오케스트레이터가 판정해 **전부 채택**했다(새 범위를 여는 것이 없고, 회원 관리는 MVP 채택 원칙) — ① 회원가입·로그인을 한 화면으로 ② 3초 카운트다운을 러닝 시작 화면 안에 ③ 탈퇴 확인 단계를 설정 화면 안에 ④ 로그인이 유지된 채 앱을 다시 열면 러닝 시작 화면 ⑤ 설정 화면 진입 출발(러닝 시작 · 랭킹 · 기록 탭) ⑥ 결과 화면 → 러닝 시작 화면 ⑦ 기록 상세 → 기록 탭(뒤로 가기) ⑧ 러닝 시작 · 랭킹 · 기록 탭 · 설정 사이 공통 탭 이동(F5 「랭킹 화면 진입」 · F7 「기록 탭 진입」 트리거가 결과 화면을 거치지 않는 직접 진입을 전제한다). 흐름표의 "러닝 진행 중 설정 화면 진입" [?] 는 "이동 없음"으로 닫았다 — 공통 탭 이동 대상에 러닝 진행 화면이 없고 이 화면에서 나가는 이동은 러닝 종료 버튼(04 F2 → F9)뿐이기 때문이다(처음에는 "04 F2 에 이동 제한 규칙이 있다"고 적었으나 원문에 없는 근거라 재감사에서 고쳤다). 「모든 화면 공통 인수 조건」 · 「디자인」 절은 저장소에 근거가 없어 [?] 로 남겼다. 반영 대상: 07 · PRD.
> (2026-09-11 6차 후속 — 같은 라운드 재감사에서 잡은 것) 재감사(BLOCKER 0 · MAJOR 5 · MINOR 12) 결과를 같은 라운드에 반영했다 — 상세 표는 docs/reviews/consistency-2026-09-11-round6.md. ① 앱 재실행 시 "진행 중" 세션이 남으면 빠져나올 흐름이 없던 교착을 **러닝 진행 화면으로 복귀해 이어서 측정**(닫혀 있던 동안은 GPS 끊김 구간과 같이 제외 · P2)으로 판정해 07 흐름표 · 04 F2 · 05 러닝 세션 상태값 · 06 세션 상태에 넣었다(자동 종료는 후순위라 쓰지 않았다). ② 가입 중 이탈 후 재진입 경로를 04 · 07 에서 맞췄다. ③ 결과 화면 · 기록 탭의 닉네임 표시를 R22("본인의 닉네임을 표시하는 모든 화면") 근거로 04 F7 · F9 에 올렸다. ④ 06 칸 쓰는 기능(세션 상태 F12 · F13, 계정 상태 F6 · F7 · F11, 사용자 칸 F13), P11 대상 F(F10), 05 로그인 전이(탈퇴), P2 「언제」, 탈퇴 삭제 범위의 06 위임, "회원가입 화면" 표기, 02 mermaid 아래 안내 줄, 07 · PRD 확정 범위, PRD 안 만드는 것의 01 MVP 제약 금지 항목을 반영했다. 남은 것(GPS "신호 없음"/"끊김" 표기, 러닝 중 페이스가 현재/평균 중 무엇인지 등)은 기록 파일에 적었다. 반영 대상: 02 · 04 · 05 · 06 · 07 · PRD.
> (2026-09-11 7차) 팀 결정 — **러닝 중 화면은 현재 페이스, 러닝 종료 후 결과 화면은 평균 페이스**를 보여준다. 07 은 러닝 진행 화면의 「보이는 것」을 "러닝 세션 · 평균 페이스"에서 "현재 페이스(저장 항목 아님 · 06 비기능 절)"로 고쳤다. 결과 화면 · 기록 탭 · 기록 상세는 이미 평균 페이스라 그대로다. 반영 대상: 01 · 02 · 03 · 04 · 06 · 07 · PRD (이슈 #21).

---

## PRD.md

> PRD.md 는 「1. 요구사항 확정」 절만 있다(뒤 절은 다음에). 문서 안에 「검토했으나 제외」 표를 둔다.

### 참고 이력

> (2026-09-11 6차) planning-partner 일괄 작성으로 「1. 요구사항 확정」 절을 신설했다. MVP 범위는 F1~F13 이다 — 기능 이름은 04 트리거 열 그대로, P 는 05 「대상 F」 를 역으로 모은 값이다(04 「정책(P)」 열은 여전히 공란). 안 만드는 것은 00-backlog 01~05 섹션과 06 「검토했으나 제외」 에서 옮기되 "전체 복원" 행과 현행 항목으로 통합된 행은 뺐고, 섹션마다 반복되던 같은 후보(목표 설정 · 음성 페이스 안내 · 주간·월간 랭킹 · TOP100 · 랜덤 닉네임 등)는 한 줄로 합쳐 출처를 모두 적었다. 구F25 행은 전체 복원이지만 그 안의 "GPS 끊김 구간 특수 표시" 세부만 후순위 유지라 따로 한 줄로 뺐다. 열린 질문은 06 의 [?] 2건과 07 의 [?] 전수다. 반영 대상: PRD.
> (2026-09-11 6차 후속) 재감사 반영 — 「안 만드는 것」에 01 MVP 개발 제약 사항의 금지 항목(다른 유저 프로필 보기 · 피드 · 댓글 · 채팅 · 전국 단위 랭킹)을 더했고, F8 기능 이름을 04 트리거 변경(로그인 화면 진입)에 맞췄으며, F10 의 P 열에 P11 을, 머리말에 확정 범위(1절)와 [?] 셈법을 적었다. 반영 대상: PRD.
> (2026-09-11 7차) 팀 결정 — **러닝 중 화면은 현재 페이스, 러닝 종료 후 결과 화면은 평균 페이스**를 보여준다. PRD 는 열린 질문에 06 의 새 [?](현재 페이스 계산 구간)를 더했다(머리말 [?] 12 → 13). 반영 대상: 01 · 02 · 03 · 04 · 06 · 07 · PRD (이슈 #21).
