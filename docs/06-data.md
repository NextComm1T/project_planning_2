# 저장 항목 — 탄천 로컬 개인 랭킹전

> 문서: 06-data.md · 상태: 초안 · 열린 질문 [?] 2개 · 채택한 [제안] 0개(일괄 작성 · [제안] 6개 판정 대기) · 마지막 갱신: 2026-09-11
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.
> 범위: 04-features.md 본문에 남아 있는 F1~F10 전부를 MVP로 보고, 그 기능이 쓰는 저장 항목만 뽑았다. 칸의 한도·규칙에는 05-policy.md의 P 번호를 함께 적었다.

---

## 저장 항목

| 항목 | 칸 (한도 · 규칙 · 출처 P) | 예시 값 | 쓰는 기능 (F) |
| ---- | ------------------------ | ------- | ------------- |
| 사용자 | 소셜 로그인 제공자 — 카카오 또는 구글 중 하나, 인증 성공 시에만 저장(F8). 인증 실패·취소 시 사용자 자체가 만들어지지 않는다 | `카카오` | F8 |
| 사용자 | 소셜 계정 식별자 [제안] — 제공자가 준 고유 값. 재로그인 때 같은 사용자를 찾는 데 쓴다 · 근거: F8은 "인증 성공 시 회원가입 완료"까지만 적혀 있고, 다시 들어온 사람을 같은 계정으로 잇는 값이 04·05에 없다 | `kakao_1029384756` | F8 |
| 사용자 | 닉네임 — 필수 입력, 미입력이면 다음 단계로 진행 불가(F10). 다른 사용자와 중복 불가(R15·F10). 랭킹에서 다른 사용자에게 보이는 유일한 값이며 그 외 프로필·상세 정보는 노출하지 않는다 · P5·SP1. 러닝 중·결과 화면 지도(F2·F9)에도 본인의 위치·경로만 표시하고 다른 사용자의 실시간 위치는 노출하지 않는다 · SP1. 9자 이하, 특수문자·공백 불가(한글·영문·숫자만) · P10 | `탄천불주먹` | F5, F9, F10 |
| 사용자 | 가입 일시 [제안] — 회원가입이 완료된 시각 · 근거: F8·F10이 가입 완료 시점을 다루지만 그 시각을 남기라는 말은 04·05에 없다 | `2026-09-01 21:03:11` | F8, F10 |
| 러닝 세션 | 어느 사용자 — 본인 세션의 결과·기록만 조회할 수 있다 · P4·P6 | `탄천불주먹` | F1, F2, F6, F7, F9 |
| 러닝 세션 | 세션 상태 — 시작 전 → 진행 중 → 종료(05 상태값). 시작 전→진행 중 전이는 GPS 확인 완료 후 시작 버튼 클릭 시에만 · P1. 한 계정은 "진행 중" 세션을 동시에 1개까지만 가질 수 있다 · P7. "종료" 전에는 결과 화면(F9)에 접근할 수 없다 · P4 | `진행 중` | F1, F2, F9 |
| 러닝 세션 | 일자 — 세션 단위로 일자별 저장, Zone 안팎 관계없이 저장한다 · P6 (F6 결과 · F9 결과 ⑥) | `2026-09-11` | F6, F7, F9 |
| 러닝 세션 | 총 러닝 거리 — 러닝 중 실시간 갱신(F2). GPS 신호가 끊긴 구간은 계산에서 제외하고 복구 지점부터 다시 측정 · P2. 값이 0이어도 특수 처리 없이 0으로 저장·표시(F9 ①) | `7.0km` | F2, F4, F6, F7, F9 |
| 러닝 세션 | 탄천 인정 거리 — Ranking Zone 내부에서 측정된 구간만 합산 · R11·P5. GPS 끊김 구간 제외 · P2. 시속 25km 초과 등 비정상 이동 속도 구간 제외 · P9. Zone 밖에서만 달린 세션은 0으로 저장(F6 예외) | `4.5km` | F2, F6, F7, F9 |
| 러닝 세션 | 러닝 시간 — 세션 타이머 값, 러닝 중 실시간 갱신(F2) | `00:42:10` | F2, F6, F7, F9 |
| 러닝 세션 | 평균 페이스(속력) — 세션 총 거리·시간으로 계산한 단순 속력값. 거리·시간의 0 처리나 GPS 예외 로직을 적용하지 않는다(F9 ⑤) | `5'38"/km` | F2, F6, F7, F9 |
| 러닝 세션 | 종료 타임스탬프 — 사용자가 종료 버튼을 직접 눌러 진행 중→종료로 전이한 시각(05 상태값). 동시 종료가 들어오면 서버가 이 시각 순서대로 순차 처리해 순위를 갱신한다 · P8 | `2026-09-11 07:41:22` | F5, F9 |
| 러닝 세션 | 결과 화면에 표시한 개인 순위 [제안] — 탄천 인정 거리가 0이면 "랭킹 미반영", 랭킹 데이터 자체가 없으면 "아직 랭킹 데이터가 없습니다" · 근거: F9 입력의 "누적 거리 기준 순위 계산값"은 계산값이라 세션에 남기는지가 04·05에 없다 | `12위` | F9 |
| 러닝 세션 | 개인 최고 기록 갱신 여부 [제안] — 이번 러닝으로 어떤 최고 기록이 갱신됐는지 · 근거: F9 결과의 "PR이 갱신되었다면 이 화면에서 즉시 안내한다"를 판정할 값이 04·05에 항목으로 없다 | `최장 거리 갱신` | F7, F9 |
| GPS 측정 지점 | **보존 범위 — 세션이 "진행 중"인 동안에만 유지하고 러닝 종료 시 파기한다. 개인 기록에는 수치만 남기고 GPS 경로는 저장하지 않는다 · R8·F6.** 지도는 F2(러닝 중)와 F9(결과 화면)까지만 보이며, 기록 탭(F7)의 과거 경로 재조회는 후순위(00-backlog.md R26·F25 부분 복원) | `러닝 종료와 함께 파기` | F2, F9 |
| GPS 측정 지점 | 어느 세션 — 지점이 어느 러닝 세션의 것인지 가리는 칸(F9 입력 "세션 전체 GPS 데이터") | `2026-09-11 07:00 세션` | F2, F4, F9 |
| GPS 측정 지점 | 위도 · 경도 — 지도에 현재 위치와 지나온 경로를 그리는 값이자(R19·F2) R11의 Zone 내부 판정·F4의 복구 지점 재측정 기준 | `37.4102, 127.1289` | F2, F4, F9 |
| GPS 측정 지점 | 기록 시각 — P3의 "신호 없음 2초 이상 지속" 판정과 P9의 속도 계산 기준. 저장 주기 [?] | `07:12:04.500` | F2, F3, F4 |
| GPS 측정 지점 | Ranking Zone 내부 여부 — 지점별 판정값. 지도에 Zone 경계를 함께 그리는 근거이기도 하다(R19·F2) · 판정 기준 좌표는 아래 "Ranking Zone 설정" 참고 | `내부` | F2, F9 |
| GPS 측정 지점 | 거리 계산 제외 여부 · 사유 — GPS 끊김 구간 제외(P2) · 시속 25km 초과 구간 제외(P9). 끊긴 구간은 지도에서도 위치 핀이 멈추고 경로 선이 이어지지 않는다(F2 예외) | `제외 · 속도 초과(P9)` | F2, F4, F9 |
| 개인 최고 기록 | 어느 사용자 — 본인 것만 조회 가능 · P6 | `탄천불주먹` | F6, F7 |
| 개인 최고 기록 | 최장 거리 — 러닝을 거듭할수록 동적으로 갱신(F7). 첫 러닝은 그 자체가 최고 기록이 된다(F7 예외). 세션 목록과 별개로 갱신·보관된다(F6) | `12.4km` | F6, F7, F9 |
| 개인 최고 기록 | 최고 페이스 — 갱신 규칙은 최장 거리와 같다(F6·F7) | `4'52"/km` | F6, F7, F9 |
| 개인 최고 기록 | 최대 운동 시간 — 갱신 규칙은 최장 거리와 같다(F6·F7) | `01:20:33` | F6, F7, F9 |
| 개인 최고 기록 | 각 기록을 세운 일자 [제안] — 근거: F7은 최고 기록 3항목을 "표시"한다고만 적혀 있고, 언제 세운 기록인지는 04·05에 없다 | `2026-08-24` | F7 |
| 랭킹 집계 | 어느 사용자 — 개인 단위로만 집계하며 크루·팀 단위 합산은 없다 · SP2 | `탄천불주먹` | F5 |
| 랭킹 집계 | 누적 탄천 인정 거리 — 세션의 탄천 인정 거리만 누적한다(Zone 외부 거리는 합산하지 않음) · R11·P5·P9. 마감·초기화 없이 단일 집계로 계속 누적한다(05 상태값 "집계 중"). 누적 한도 없음 · P6 | `38.2km` | F5, F9 |
| 랭킹 집계 | 최초 달성 시각 — 같은 누적 거리라면 그 거리를 먼저 달성한 사용자가 상위 · P5. 동시 종료는 종료 타임스탬프 순서대로 처리해 갱신 · P8 | `2026-09-11 07:41:22` | F5, F9 |
| 랭킹 집계 | 집계 상태 — "집계 중" 단일 상태, 주기적 마감·초기화 전이 없음(05 상태값). 집계 대상이 하나도 없으면 F5는 "아직 랭킹 데이터가 없습니다"를 보여준다 | `집계 중` | F5 |
| Ranking Zone 설정 | 구역 경계 좌표 — 시스템에 사전 설정된 값으로 취급하고 설정 주체는 문서 범위 밖(00-backlog.md 구P8 무효화). F2 입력 칸에 명시된 판정 기준이며, R11·P5·P9의 Zone 내부 판정과 R19·F2 지도의 경계 표시가 모두 이 값을 쓴다. 좌표의 형태·정밀도 [?] | `탄천 Ranking Zone 폴리곤` | F2, F5, F9 |

## 비기능 한 줄

러닝 중에는 거리·시간·탄천 인정 거리·페이스가 실시간으로 갱신되지만 순위는 갱신하지 않고, 랭킹 갱신은 세션 종료 타임스탬프 순서대로 순차 처리한다(R6·P8). 개인 기록은 Zone 안팎 관계없이 한도 없이 누적 저장한다(P6).

> [제안] GPS 신호 상태(정상 → 끊김 → 복구)는 화면 경고 표시용 일시 상태로 보고 저장 항목에 넣지 않았다 · 근거: F3의 결과·예외가 "경고 표시 / 정상 복구 시 경고 사라짐" 화면 동작만 서술하고, 새로고침 뒤에도 남아야 한다는 말은 04·05에 없다. 남겨야 한다면 "GPS 측정 지점"의 계산 제외 여부 칸으로 대신할 수 있다.

## 검토했으나 제외

| 후보 | 출처(누구 초안 · [제안]) | 제외 이유 |
| ---- | ------------------------ | --------- |
| 개인 기록의 GPS 경로 영구 저장 | R8·F6 (2026-09-11 R19 라운드) | 제외 — 지도는 F2(러닝 중)와 F9(결과 화면)까지만 보이고, F6 저장 시에는 수치만 남긴다. 기록 탭(F7)의 과거 경로 재조회는 후순위 — 00-backlog.md 의 R26·F25 「부분 복원」 행 참고 |

> 검토했으나 제외한 후보와 문서 정리 이력은 [00-backlog.md](00-backlog.md)에 모아뒀다.
