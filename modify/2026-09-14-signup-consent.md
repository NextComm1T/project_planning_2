# 2026-09-14 — 가입하기(약관 동의) 화면 · 구현 ↔ 기획 문서 차이

> 구현 기준은 캡처된 디자인(루트 `탄천런.dc.html` L95-135)이다. 기획 문서(`docs/01~07`)는 고치지 않았고,
> 나중에 한 번에 맞출 때 아래 목록을 그대로 훑으면 된다.

이번 작업: 이슈 #37 — `/signup/consent` 화면.

---

## 1. 이 화면 자체가 기획 문서에 없다

- **문서**: `docs/04-features.md:23` F8 결과 — 인증에 성공하면 바로 닉네임 설정(F10)으로 간다. 동의 단계가 없다.
- **디자인 · 구현**: `login → consent → nickname` 순서다. 개인정보 수집·이용 동의 화면을 `/signup/consent` 로 구현했다(디자인 L95-135).
- **고쳐야 할 곳**: `docs/04-features.md:23` F8 결과 · `docs/07-screens.md` 화면 목록과 흐름표.
  이미 `modify/2026-09-14.md` 2번에 같은 내용이 있다 — **문서 정리 때 둘을 함께 본다.**

## 2. route 를 `/signup/consent` 로 확정 — 닉네임 경로가 앞 기록과 갈린다

- **문서**: `docs/07-screens.md:5` — "주소(URL)는 적지 않는다".
- **구현**: 이 화면은 `/signup/consent`, 상세는 `/signup/consent/detail`(#38), 계속하기 버튼은 `/signup/nickname`(#39) 로 보낸다.
  `docs/SCREEN_ASSIGNMENTS.md` 담당 표의 경로와 같다.
- **갈리는 지점**: `modify/2026-09-14.md` 5번 표는 닉네임 화면을 **`/nickname`** 으로 적어 두었다.
  담당 표(#39)는 `/signup/nickname` 이다. 구현은 담당 표를 따랐다.
- **고쳐야 할 곳**: `modify/2026-09-14.md` 5번 표의 닉네임 행. 문서 정리 때 `/signup/nickname` 으로 통일한다.

## 3. 제공자 문구 폴백은 디자인에 없는 문장이다

- **디자인**: L105 `{{ loginProvider }} 계정으로 가입을 진행합니다.` — `loginProvider` 는 로그인 시 `Kakao` · `Google` 로 채워진다(L1146).
- **구현**: 인증이 없어 화면이 제공자를 알 수 없다. `/signup/consent?provider=kakao|google` 쿼리로 받아
  「카카오 / Google 계정으로 가입을 진행합니다.」를 띄우고, **쿼리가 없거나 모르는 값이면 「소셜 계정으로 가입을 진행합니다.」** 로 쓴다.
  이 폴백 문장은 디자인에 없다. OAuth 콜백이 붙으면 쿼리를 채우는 자리라 그대로 이어진다.
- **이번 작업에서 하지 않은 것**: 로그인 화면의 버튼 동작은 건드리지 않았다(#37 범위 밖). 그래서 지금 로그인에서
  이 화면으로 오는 경로는 없고, 주소를 직접 열어야 한다.
- **고쳐야 할 곳**: 문구를 문서에 담을지 여부. `modify/2026-09-14.md` 4번(문구 확정)과 같은 판단이다.

## 4. 동의 여부 · 동의 시각을 저장할 칸이 없다

- **문서**: `docs/06-data.md` 저장 항목에 개인정보 동의 관련 칸이 없다. `docs/05-policy.md` 에도 동의 정책이 없다.
- **구현**: 서버가 없어 동의 결과를 저장하지 않는다. 체크 상태는 화면의 지역 상태뿐이다.
- **고쳐야 할 곳**: `docs/06-data.md` 저장 항목(동의 여부 · 동의 일시) · `docs/05-policy.md`(법적 요구사항 확인).
  `modify/2026-09-14.md` 2번의 "05-policy 에 정책이 필요한지 확인"과 이어진다.

## 5. 공통 완료 기준의 네 상태가 이 화면에는 해당 없다

- **문서**: `docs/07-screens.md:12` — 모든 화면은 불러오는 중 · 빈 상태 · 오류 · 정상 네 상태를 구분한다.
- **구현**: 이 화면은 **원격 데이터도 서버 요청도 없다.** 약관 문구는 코드 상수이고 필수 항목은 항상 1개다.
  기다릴 대상도, 비어 있을 대상도, 실패할 요청도 없어 **「정상」 하나뿐**이다(동의 전 / 동의 후 두 모습).
  없는 오류를 흉내내는 UI 나 새 쿼리 계약을 만들지 않았다 — 팀 결정(2026-09-14).
- **채울 시점**: 서버가 붙어 동의 저장이 생기면 그때 저장 실패 상태를 만든다.
- **문서 변경**: 불필요. 다만 네 상태 기준이 "해당 없으면 없는 것"임을 문서에 한 줄 적을지는 판단 필요.

## 6. 상세 화면에 다녀왔을 때 동의 상태 유지는 #38 에서 연결한다

- **#38 의 인수 조건**: 상세(`/signup/consent/detail`)에서 돌아왔을 때 동의 상태가 유지된다.
- **이번 구현**: 하지 않았다. 상세 화면이 아직 없어 **유지되는지 확인할 방법이 없고**, 서버 저장·전역 상태는 이번 범위 밖이다.
  동의 상태는 `ConsentForm` 의 `useState` 뿐이고, 상세로 가는 화살표는 평범한 `<Link>` 라 어떤 방식이든 나중에 얹을 수 있다.
- **#38 에 넘기는 방법(제안)**: `src/app/signup/consent/layout.tsx` 에 클라이언트 Context 를 두면
  `/signup/consent` ↔ `/signup/consent/detail` 이동 사이에 layout 이 유지되므로 동의 상태도 살아남는다.
  이 파일은 가입 화면 폴더 안이라 다른 사람 작업과 충돌하지 않는다.

## 7. 클릭 영역 — 토글과 상세 이동을 분리했다

- **디자인**: L109(체크 네모) · L111(항목명)은 같은 핸들러(`onToggleConsent`), L115(화살표)만 `onOpenConsentDetail` 이다.
- **구현**: 체크 네모와 항목명을 `<button role="checkbox">` **하나**로 묶고(버튼 안에 버튼을 넣지 않는다),
  화살표는 형제 `<Link>` 로 뒀다. 두 영역은 겹치지 않는다 — 실측으로 확인했다(토글 오른쪽 끝 309px = 화살표 왼쪽 끝 309px).
- **터치 영역**(`docs/07-screens.md:15` 최소 48×48): 두 조각 모두 보이는 크기는 디자인 그대로 두고,
  **음수 margin 으로 레이아웃 높이를 되돌리면서 클릭 상자만 48×48 로** 키웠다.
  토글은 `py-[11px]` + `-my-[11px]`(26 → 48), 화살표는 `size-12` + `-my-2 -mr-2`(32 → 48).
  덕분에 카드 높이(68px)와 화살표 중심(오른쪽에서 36px)이 디자인과 같은 값으로 남는다.
  `BackButton` 의 `after:-inset-*` 방식은 투명 영역이 옆 토글 위로 번져 쓰지 않았다.
- **문서 변경**: 불필요.

## 8. 디자인 원본 파일의 렌더 아티팩트 2가지 — 따라가지 않았다

`탄천런.dc.html` 을 브라우저로 열어 구현과 **실측 비교**(390×844 뷰포트)한 결과, 디자인 파일이 브라우저에서
그려지는 모습과 디자인의 의도가 어긋나는 지점이 두 군데 있었다. 둘 다 **의도를 따랐다.**

| 아티팩트 | 디자인 파일이 실제로 그리는 것 | 구현 | 왜 |
| --- | --- | --- | --- |
| 버튼 안 글자 | `[필수] 개인정보 수집·이용 동의` · `개인정보처리방침 보기` 가 **Arial** 로 나온다 | Pretendard | 디자인 마크업이 그 `<button>` 에만 `font-family` 를 빠뜨렸다(L111 · L122). 한글을 Arial 로 그리는 건 의도가 아니다 |
| 아이콘 굵기 | 모든 SVG 가 **stroke-width 1px** 로 나온다 | 2.4 · 3.2 | 원본이 React 식 `strokeWidth` 를 쓰는데 브라우저가 무시한다. 값은 소스에 그대로 적혀 있고, 이미 머지된 `BackButton`(2.4)도 같은 값을 쓴다 |

- **문서 변경**: 불필요. 디자인 파일을 눈으로 볼 때 이 두 가지는 실제 앱과 다르게 보인다는 점만 팀이 알면 된다.

## 9. 화면 아래 여백 22px — 디자인 목업의 노치는 빼고 맞췄다

- **디자인**: 콘텐츠 영역이 `padding:22px 20px`(L101) 라 계속하기 버튼 **아래로도 22px** 이 남고,
  버튼 블록 자체의 아래 여백 10px(L129)과 합쳐 화면 바닥까지 32px 이다.
- **구현**: 처음에 위쪽 22px 만 넣어 버튼이 22px 아래로 내려갔다. 실측으로 잡아 아래 22px 을 채웠다(바닥까지 32px 로 일치).
- **남는 차이**: 디자인 목업은 프레임 안에 **상태바(시각 31px)** 를 그려서 콘텐츠 높이가 813px 이고, 실제 앱은 844px 이다.
  그래서 목업에서는 버튼 위 빈 공간이 31px 더 좁다. 이건 기기의 노치 영역이라 따라가지 않는다 —
  실제 기기에서는 `AppShell` 의 safe-area 처리가 담당한다.
- **문서 변경**: 불필요.

---

## 이번 작업에서 구현하지 않은 것

- 동의 결과 저장 · 실제 인증 연동 (서버 없음)
- `/signup/consent/detail`(#38) · `/signup/nickname`(#39) · `/privacy-policy`(#48) — 링크만 있고 화면은 아직 없어 **404** 다.
  로그인 화면의 "개인정보처리방침 보기"가 지금 404 인 것과 같은 상태다.
- 로그인 화면에서 이 화면으로 넘어오는 연결(로그인 버튼 동작 수정은 #37 범위 밖)
